# 凭据与运行时 home 模板。**可选** —— 只有想用「服务端兜底 Key」时才需要。
#
# 用户自带 Key 模式（默认）下，人们在界面里填自己的 Key 即可，本文件可以不建。
# 但若希望"不填 Key 也能用"，就在这里配一个兜底 Key：
# 此时响应里会带 meta.key_source="server"，前端显示「本次使用服务端 Key」。
#
# 为什么单独一个文件：dsh 会扫描 workspace 根目录的 .env 并拒绝其中的
# DSH_* 与凭据变量，所以这些「启动环境专属」的值必须放在 dsh 不扫描的
# 文件里，由 core/app/config.py 读取。

# ===== 服务端兜底凭据（可选）=====
# 留空/删掉这一行 → 必须由用户在界面上自带 Key，否则接口返回 NO_API_KEY。
DEEPSEEK_API_KEY=sk-在此填入你的key
# 走兼容代理端点时取消注释
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ===== DSH 运行时 home（仅 TA_BACKEND=dsh 时需要，纯英文绝对路径）=====
# direct 后端（默认）不使用这个目录。
# 不设置会继承 DSH 主环境的 DSH_HOME，导致 SDK 往主环境写 profile 与会话。
DSH_HOME=D:\work\tea-advisor\dsh-home

# ⚠️ 安全提醒：本文件含明文凭据，绝不可入库（已被 .gitignore 忽略）。
# 若你把服务暴露到局域网/公网，任何能访问它的人都能消耗这个 Key 的额度。
# 那种场景下请留空此文件，改为强制用户自带 Key。

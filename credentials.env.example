# 运行配置模板。**可选** —— 不建这个文件也能跑（全部有默认值）。

# 本项目**不保存、也不读取任何 API Key**。
# Key 只有一个来源，由调用方逐请求提供：
#   · 网页版：页面上的「API Key」一栏 → Authorization: Bearer <key> 头
#   · 终端与脚本：环境变量 DEEPSEEK_API_KEY
#         $env:DEEPSEEK_API_KEY = "sk-..."
# 所以本文件里**不应该**出现 DEEPSEEK_API_KEY —— 写了也不会被读取。

# 为什么仍然要单独一个文件（而不是写在 .env 里）：
#   dsh 启动时会扫描 workspace 根目录的 .env，发现任何 DSH_* 变量（含 DSH_HOME）
#   就拒绝启动。把这类「启动环境专属」变量放在它不扫描的文件名下，
#   由 core/app/config.py 读取、core/app/agents/runtime.py 注入子进程环境。

# ===== 自定义接口地址（可选）=====
# 走兼容代理端点时才取消注释。默认 https://api.deepseek.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ===== DSH 运行时 home（仅 TA_BACKEND=dsh 时需要）=====
# 默认的 direct 后端不使用这个目录（也不启动任何子进程）。
# 必须是纯英文绝对路径；不设会继承 DSH 主环境的 DSH_HOME，
# 导致 SDK 往主环境写 profile 与会话。
DSH_HOME=D:\work\tea-advisor\dsh-home

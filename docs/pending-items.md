# 挂起事项清单

> **这份清单是挂起事项的唯一真源。** 其他地方（`handover.md`、`maintenance.md`）只提摘要，
> 一律以本文为准。任何一项解决了，就在这里更新状态并记录解决方式。
>
> 快照时间：**2026-09-20**　共 **55** 项记录在案（A/B/C/D/E 五类），
> 其中 **A2、B2、D2、D13、E1、E2、E3、E12、E15 已解决**、**D18／D23／D25 已实施**（阶段 0／1／2 落地，见各条详情；D18 的阶段 3「近 24 小时累积」**独立立项**，不占本清单的待处理数）、**D20／D22 经裁定关闭**（发物属「事前提醒」逻辑，本项目只做「事后处理」）、**B3 的阻塞已解除**（九型已上线；244 格的布局中已落地 17 格，余 227 格是完备度而非功能阻塞）、**D12 经裁定冻结**（保留短 `note` 白名单与守卫，不补长；等 A1 审核那轮顺手处理）、**D31 已实施**（(β) 改掉「夜里吃得多」的叙述；(α) 加时段判据依赖 D28，仍待定），其余 **38** 项待处理。

---

## 概览

| ID | 事项 | 谁来解 | 阻塞什么 | 状态 |
|---|---|---|---|---|
| **A1** | 食性表 147 条全部未人工审核 | 具备资质的中医师/中药师 | ⛔ **对外提供服务** | 待人工 |
| **A2** | A1 核对遗留 4 项待确认（酱油 + 3 味菇等） | 项目所有者 | 不影响功能，影响 3 条数据的最终取值 | ✅ **已解决**（2026-09-18） |
| **A3** | `口蘑` 无来源行却挂在 `mogu` 别名上（凭项目判断保留） | 具备资质的中医师/中药师 | A1 的审核质量（「无证据却挂别名」的状态） | 待确认 |
| **B1** | 7 处药典不一致待复核 | nanple | 数据准确性（不阻塞功能） | 待 nanple |
| **B2** | 问卷子包 `phlegm_dampness` 拼写不一致 | 开发 | 问卷接入 | ✅ **已解决**（2026-09-18，方案① 问卷侧改名） |
| **B3** | 244 个配伍判定（九型已上线，余下是完备度；第三批落地后余 227 格） | nanple | 推荐质量（不再阻塞体质模型 5→9 型） | 已部分解决 |
| **C1** | 《中华本草》列全空 | 需取得资料 | 三源交叉验证 | 待外部资料 |
| **C2** | 国标 A 栏是转引，非标准正文 | 需取得资料 | 逐字对齐国标 | 待外部资料 |
| **C3** | 引用的药典是 2020 版，已被 2025 版废止 | 需取得资料 | 跨版性味归经是否变动未经核实 | 待外部资料 |
| **D1** | 玫瑰花/茉莉花的代码改动 | 项目所有者定时机 | 合规处置落地 | 待实施 |
| **D2** | 本地提交未推送 | 项目所有者 | 协作者看不到这批产出 | ✅ **已解决** |
| **D3** | 7 味须预处理（捣碎/掰开）未纳入本轮 | 项目所有者定范围 | 不影响判定，影响这 7 味的出味效果 | 待确认 |
| **D4** | 「白酒」的 `keywords` 混入「黄酒」（另一物、四气差一档） | 项目所有者 | `baijiu` 的匹配与四气呈现准确性（以 0.9 呈现一个不属于它的四气） | 待确认 |
| **D5** | 「糖」的 `aliases`/`keywords` 混入「红糖」，而红糖应另立条目（另一物、四气差一档） | 项目所有者 | `tang` 的匹配与四气呈现准确性；红糖暂无条目 | 待确认 |
| **D6** | 「葱」是否拆成葱白/葱叶（两处依据对部位的判法不一致） | 项目所有者 | `cong` 的粒度；叶用部位的四气归属 | 待确认 |
| **D7** | 派生规则缺「温度 / 介质」两个变量（冷热之外，还受「怎么吃、跟什么一起吃」影响） | 项目所有者定口径 | B4 派生规则的完备度；不阻塞现有判定 | 待确认 |
| **D8** | 派生规则缺「食用场景」维度（空腹/夜宵/配酒等场景下的偏性） | 项目所有者定范围 | 同上；与 `RULES.late_night` 那条既有场景规则相邻 | 待确认 |
| **D9** | 反向查询未做（按四气反查食物：给我寒凉的） | 项目所有者定优先级 | 一个独立入口；现有链路只有「食物 → 四气」单向 | 待确认 |
| **D10** | 用户反馈闭环未做（用户说「上次那个不可信」时无处落） | 项目所有者定范围 | 数据治理的输入来源；A1 的审核进展现只靠内部标记 | 待确认 |
| **D11** | 「寿司 / 咖喱」是否按子类型拆条 | 项目所有者 | 两条的粒度；生食款与熟食款是否该同档 | 待确认 |
| **D12** | 13 条旧茶饮的 `note` 仅 4–9 字（`note` 温度通道对它们是**开着的**） | 项目所有者定范围 | 这 13 条的四气**只靠运气**不受 `note` 影响；B4 的 ≥18 字纪律没覆盖它们 | ✅ **已关闭**（2026-09-21 裁定：冻结现状） |
| **D13** | 核验单不渲染 B4 的 `note` 与冲突登记 | 开发 | A1 审核人以核验单为依据，却看不到派生依据与 4 条待裁定冲突 | ✅ **已解决**（2026-09-19） |
| **D14** | 大麦茶的「制品口径」未定（B4 判 L1 vs B5 判 ③ 两种说法并存） | 项目所有者 | `damaicha` 的四气归属与判层（现值不动） | 待确认 |
| **D15** | B5 新增的 2 条 ② 口径分歧（橘子／猪肉）等裁定 | 具备资质的中医师/中药师 | 这 2 条的最终取值（值一律未动） | 待审核 |
| **D16** | `suantou` 的 `id` 与 `name` 不一致（id 指向蒜、name 却是「米粉」，疑似既有笔误） | 开发 | 按 id 取条目会拿到另一个物；**登记不改**，改法待定 | 待确认 |
| **D17** | B5 新发现的 2 条别名同族混入（橘子含「橙子／柑」、鱼含「鲈鱼／草鱼」） | 项目所有者 | 以 0.9 呈现不属于该条的四气（与 D4／D5 同族） | 待确认 |
| **D18** | 冲突处理框架（冲击度轴 + 体质×极端轴）的落地实施 | 开发（阶段 0／1／2）；阶段 3 独立立项 | 方向相反的一餐与体质×极端场景目前无确定性处置 | ✅ **已实施**（2026-09-20，阶段 0／1／2；阶段 3 独立立项） |
| **D19** | 整餐偏性是否收归确定性（接线 `nature_math.combine()`） | 项目所有者 | 整餐寒热现由模型自拍或离线关键词打分，无确定性合成 | 待确认 |
| **D20** | 发物名单落地（有本地依据的上位类别可落；表外项记 not_in_catalog） | 项目所有者定依据来源 | 特禀 × 发物（C 轴硬禁忌支）无数据可依 | ✅ **已关闭**（2026-09-20 裁定：发物属事前提醒，不单列） |
| **D21** | 食物侧缺体质消费通路（`check_constitution_fit` 只读饮片） | 项目所有者定范围 | D18 压在它上面（原并列的 D20 已关闭） | 待确认 |
| **D22** | 硬拦截的落地形状（原口径「提示 + 方向置顶」） | 开发 | 特禀 × 发物标了「硬禁忌」却没有实现形状 | ✅ **已关闭**（2026-09-20 裁定：本项目不做拦截） |
| **D23** | 湿气生成度落地（先落高糖／高油／生冷／乳制品 4 维；黏腻见 D27） | 开发（阶段 0／1／2） | 「今天要不要额外加化湿、加多少」无确定性依据 | ✅ **已实施**（2026-09-20，阶段 0／1／2） |
| **D24** | 信号结构升级：原子信号只定义一次，每个维度各带权重与聚合 | 开发（阶段 0） | 冲击度与湿气度共用同一批信号；两维各建识别器必然漂移 | 待实施（口径已定） |
| **D25** | 湿气轴取代 `RULES.sweet_heavy`（消除两套化湿逻辑） | 开发（阶段 2） | 现状「甜腻 → 化湿」已有实现，不接管就是两套并存 | ✅ **已实施**（2026-09-20，阶段 2；`sweet_heavy` 已从 `RULES` 移除） |
| **D26** | 份量解析（`AnalyzeRequest` 无份量字段，「大量」无来源） | 项目所有者定范围 | 「一小块蛋糕」与「三块奶油蛋糕加冰奶茶」目前命中同一规则、同一搭配 | 待确认 |
| **D27** | 黏腻维度缺食物（食物表 147 条内无任何糯米制品） | 项目所有者定范围 | 湿气轴 5 维里 1 维落不了（扩食物表属另一条线） | 待确认 |
| **D28** | 终端离线路径把 `meal_time` 写死 `UNKNOWN`（与 API 离线路径的猜测不一致） | 项目所有者定范围 | 场景规则（含夜宵）在终端路径下永远命不中 | 待确认 |
| **D29** | `鸡蛋` 的 `keywords` 含裸字「蛋」⇒ 输入「蛋糕」同时命中蛋糕与鸡蛋 | 项目所有者定范围 | 以 0.9 呈现不属于该条的四气（与 D4／D5／D17 同族、成因不同） | 待确认 |
| **D30** | 「冰奶茶」被同时识别成「冰奶茶」与「奶茶」两条（重复计入同一餐） | 项目所有者定口径 | 展示重复、置信度均值被拉偏；阶段 1 后冲突判定会重复计一次四性 | 待确认 |
| **D31** | `RULES.late_night` 靠**空关键词**兜底，把**非夜宵**的一餐说成「夜里吃得多」 | 项目所有者定口径 | 未触发任何方向时（老路）会给出一句**凭空的时间叙述**；阶段 2 只在自己触发的分支上绕开它 | ✅ **已实施**（2026-09-21，(β) 改叙述；(α) 待 D28） |
| **E1** | 3 条食性表缺 `review_status` | 开发 | 无（行为已正确） | ✅ **已解决**（2026-09-17） |
| **E2** | `herbs.json` 饮片条目缺逐条审核字段 | 开发 | A1 的验收标准落不到 herbs | ✅ **已解决（结构）**（2026-09-20） |
| **E3** | 饮片侧缺「逐味公开依据」链（分叉批次评估的缺口 3） | 开发 | 用户与审核人看不到每味饮片的出处 | ✅ **已实施**（2026-09-18） |
| **E4** | LLM 路径与离线路径对**主导体质**的 `unsuitable_for` 处置不一致（硬排除 vs 软提示） | 项目所有者定口径 | 同一体质有没有 Key 拿到的安全边界不同 | 待确认 |
| **E5** | 非继承类 `how` 的 `matched_name` 缺守卫（「同条目·品种方向描述」+ 既有「等价名（通称差异）」） | 开发 | 这两类来源依据在登记表里无自动核验 | 待补 |
| **E6** | 路线图文档的数字已失真（仍写「能引来源 35／无来源 65」「L2 32 + L3 33」） | 开发 | 后续批次的范围判断（后来人会按它排期） | 待修正 |
| **E7** | 麦冬 × 阴虚这一格（E 级「宜」）判定了但**没有用户可见出口** | 开发（出口待定） | 该格结论只留痕在数据里，用户与审核人都看不到 | 待确认（α／β 二选一） |
| **E8** | 昆布与食物表 `haili`（海带）跨库重复（同一物两处取值） | 项目所有者裁定 | 是否立饮片条目、以哪一处为准 | 待确认 |
| **E9** | 化橘红与既有 `juhong`（橘红）近似名冲突（品种上是另一物） | 项目所有者裁定 | 是否立条目、与 `juhong` 如何区分 | 待确认 |
| **E10** | 金银花的「做法依据」缺语料原句（定为泡水，却无「泡服／代茶饮」出处） | 具备资质的中医师/中药师 | 该味做法口径的可核查性（含 `requires_cooking` 取值的依据） | 待确认 |
| **E11** | 金银花与既有 5 味凉／寒饮片的**湿热口径并存**（粤中医未点名 vs 判据成文前自拟） | 项目所有者裁定 | 同族饮片对湿热质的口径一致性 | 待确认 |
| **E12** | `docs/handover.md` 写「34 味食性表」（**语义笔误**：34 味指饮片，食性表是 147 条） | 开发 | 交接文档可信度（会让人低估 A1 的 147 条规模） | ✅ **已解决**（2026-09-20） |
| **E13** | `cautions` ↔ `unsuitable_for` 的一致性守卫**只覆盖阴虚方向**（阳虚／痰湿方向无守卫） | 项目所有者定范围 | 另两个方向的「caution 提了却不屏蔽」无人守 | 待确认 |
| **E14** | 2 处 `cautions` 与 `unsuitable_for` **软硬脱节**（青果×阳虚、红枣×痰湿） | 项目所有者裁定 | 条目内部自相矛盾（界面提示与候选集相反） | 待确认 |
| **E15** | 离线路径把整句口述当 `note` 喂进温度通道（既串味、又换条目） | 开发 | 离线验证的准确性；新机制的离线验证全依赖它 | ✅ **已解决**（2026-09-20） |

**⛔ 标记的是真正的硬阻碍**：只要 A1 没解决，「对外提供服务」就不能说完成。
（B3 原先也标 ⛔，2026-09-17 九型上线后已解除——244 格里余下的 227 格影响的是推荐完备度，不再是功能阻塞。）

---

# A 类 · 对外服务的硬阻碍

## A1　食性表 147 条全部未人工审核

| | |
|---|---|
| **现状** | `food_properties.json` 的 147 条（129 食材 + 18 茶饮），逐条 `review_status` **全部是 `pending`**，没有一条 `approved` |
| **范围** | ⚠️ **本项覆盖两张表。** `food_properties.json` 的 147 条逐条 `review_status` 全 `pending`；`herbs.json` 的饮片条目**原先没有逐条审核字段**（审核状态只在文件级 `_meta.review_status`），该**结构**缺口已于 2026-09-20 补齐（**E2**：逐条 `review_status: pending` ＋ `reviewed_by` / `reviewed_at` / `review_note`）。**两张表合起来没有一条 `approved`** —— 饮片那批的逐条审核动作属本项范围，不另立挂起项 |
| **影响** | 所有条目虽按 0.9 置信度参与判定，但界面**必须**标「待验证」。这是刻意的保守设计，不是 bug |
| **为何只有人能解** | `approved` 意味着「真·硬规则库、界面不再标注」，必须由具备资质的中医师/中药师逐条确认，并同时填写 `reviewed_by` / `reviewed_at` |
| **需要什么** | 审核人 + 一份逐条审核流程（`docs/maintenance.md` §9.2/§9.3 有正确姿势）。**核验单已就绪**：`docs/food-properties-review-sheet.md`（脚本生成、只读可重跑）。④ 层内部矛盾已清零（4 类于 2026-09-17 修完）。①②③ 层按「官方优先 + 多源兜底 + 无源标空」推进，**第一批已定为「蔬菜/调味/水果中有来源可引的 33 条」**（**① 28 一致 + ② 4 冲突 + ③ 1 无源**），清单见 `docs/food-properties-batch2-confirm.md`。**第二批（B2，2026-09-19）已登记主食/制品 12 条**（一致 5 + ② 口径分歧 7，均标 `batch=后续`），见 `docs/food-properties-b2-staple.md`；其中 7 条口径分歧同样**等审核人裁定**，值与 `note` 一律未动。**第三批（B3，2026-09-19）已登记网络权威来源 5 条**（① 一致 2 + ② 口径分歧 3），见 `docs/food-properties-b3-network-sources.md`。**第四批（B4）已执行（第 2–5 步）**：六条规则成文 + 逐条推导 + 逐条 `note` 见 `docs/food-properties-b4-derivation.md`（L2 实测 **53** 条 = 一致 49 + 冲突登记 4；`kafei`/`qiaokeli` 退出 L2、`damaicha` 归 L1）；修订方案与拍板过程见 `docs/food-properties-b4-derivation-plan.md`。**落点**：规则真源 `_meta.nature_derivation_rules`（新键，**不进提示词**）、冲突登记 `_meta.derivation_conflicts`、53 条 `note`（走 `patch_food_table.py`）、6 组守卫 12 条测试（变异检验 10/10 全红）。⚠️ **`nature` 一行未动**。遗留 D12／D13。<br>**第五批（B5，2026-09-19）已执行**：无覆盖条目收口 **19 条**（原估 33 条 —— B4 把一批原判 ③-b 的条目收编成了派生条目，故边界须从当前数据反推）；登记表 75 → **94** 条：14 条语料无条目 ＋ 1 条制品口径未定（大麦茶）＋ 1 条通称无条目（鱼）判 ③，橘子／猪肉判 ② 口径分歧（值不动），虾判 ① 一致。逐条证据、边界修正与顺带发现见 `docs/food-properties-b5-l3.md`。<br>⚠️ **原定第一批「★ 主食/水产/乳饮」已作废**——那 29 条多为加工食品（米饭、饺子、奶茶），50 号文件 §5.2「真无据清单」本就覆盖它们，是**结构性**无来源，不是核对不到位。核验单的「第一批」判据已改为**来源登记表里 `batch=第一批`**（`build_food_review_sheet.py` 里写死类别的 `FIRST_BATCH_CATEGORIES` 已删除），不再按类别判定 |
| **验收标准** | `food_properties.json` 的 `review_status` 分布中 `approved` 条数 > 0，且每条 approved 都有 `reviewed_by` + `reviewed_at` |
| **注意** | ⚠️ **不要为了界面好看而批量置 approved**。标记密度就是审核进度的可见反馈，批量置等于把这个反馈抹掉 |
| **相关文件** | `core/data/food_properties.json`、`docs/food-properties-review-sheet.md`（核验单，脚本生成）、`core/scripts/build_food_review_sheet.py`（生成工具，**只读**，`--check` 可进 CI）、`docs/food-properties-sources.json`（75 条来源登记，①②③ 层的事实源）、`docs/food-properties-batch2-confirm.md`（第一批 33 条清单，待确认）、`docs/food-properties-batch2-plan.md`（核对方案） |

## A2　A1 核对遗留的 4 项待确认（酱油 + 3 味菇）

> **✅ 已解决（2026-09-18）。** 属于 A1 的一部分，但**不能用「来源核对」解决**——都是
> 「数据该不该改」的决定，所以当初单列出来，免得混在 33 条清单里被当成已结案。
> 方案与逐条论证见 `docs/food-properties-a2-plan.md`（§8 是最终执行稿）。
>
> 落地分两次提交：**提交 A**（`docs/food-properties-sources.json` 措辞归一 + `_meta.controlled_vocab`
> + 核验单 §4.4 加「限定(how)」列 + 把数据快照断言改成派生不变式）→ **提交 B**
> （`core/scripts/patch_food_table.py` 的 `ENTRY_FIXES` 改 `jiangyou` / `mogu` → 数据 →
> 守卫测试）。数据改动全程走补丁脚本，**没有手改** `food_properties.json`。

| | |
|---|---|
| **① 酱油（`jiangyou`）** | ✅ **映射采纳、值不改**。依据《本草纲目》「酱」记「辛、温」，但 50 号文件 §5.4 自定「仅经典有载、教材无（**不得当教材用**）」——纲目强度低于教材，**据此改判不成立**，故 `nature` 保持「平」。数据侧补 `note` 留痕（此前 4 条口径分歧里**只有它没留痕**，而核验单 §4.2 写着一律加 note）；`sources.json` 里 `accepted` 由 `null` 改 `true`、`open_question` 置 `null`。守卫：`core/tests/test_food_lookup.py` 的 `test_jiangyou_note_has_no_temperature_side_effect` |
| **② 金针菇 / 杏鲍菇 / 平菇** | ✅ **走 (C)：从 `aliases` + `keywords` 两处移出，不立新条目**。这 3 个物种 50 号文件**无条目**，原先挂在 `mogu` 上随它被判为**寒** —— 即**以 0.9 置信度呈现一个无来源的四气**；移出后走「表未覆盖」的模型推测分支（0.3，界面标「食性未经验证」）。**必须两处都清**：`_find_entry` 的第 2 级把 `keywords` 与 `aliases` **合并扫描**（`core/app/services/food_lookup.py`），只删一处无效 |
| **③ 蘑菇拆条的收尾** | ✅ **`mogu` 不改名**（`name` 仍为「蘑菇」）：改名会让用户说「蘑菇」落到别名匹配、并可能启用烹饪修正层（`exact_name_hit` 的分支决定判层），属「改名打断匹配」同族风险。`xianggu` 保持 `batch=后续`（**不把第一批从 33 改成 34**），`review_status: pending`、**不继承**原 `mogu` 的任何审核痕迹（拆出的新条目从零开始） |
| **④ C 组 4 条来源值的限定写法** | ✅ **定受控词表**：`_meta.controlled_vocab` 固定 `how` 6 值 / `tier` 5 值 + `tier→source_registry` 对应；13 处措辞归一（8 处混用值「别名/等价名」拆开、目录档 4 处「正名」纠为「别名」、1 处归一）。核验单 **§4.4 新增「限定(how)」列** —— 此前 `how` 根本不渲染，限定形同虚设；且 `reference` 里**不含** `how`，取值须按 `(source_id, matched_name)` 回 `evidence[]` 找（同一条目常有多条依据，取 `[0]` 会错）。限定**只留在 `sources.json`**，不给生菜/蒜补运行时 `note`（`note` 会改四气，实测全表 4 条命中，`suannai_wan` 就是从 note 改的档） |
| **相关文件** | `docs/food-properties-sources.json`、`docs/food-properties-review-sheet.md` §4.2/§4.4、`core/data/food_properties.json`、`core/scripts/patch_food_table.py`、`docs/food-properties-a2-plan.md`（方案 + 执行结果） |

> ⚠️ A2 收口**顺带生出**一条新挂起项：`口蘑` 无来源行却挂在 `mogu` 别名上 → 见 **A3**。
> 另有两条顺手修掉的「文档说了、事实不符」：核验单 §4.2 的「一律加 note」现在由守卫测试
> 把关（`core/tests/test_food_review_sheet.py` 的 `test_first_batch_conflicts_have_note`）；
> `sources.json` 的 `outcome`/`scope` 曾把第 18 条 `xianggu` 漏在计数外（17 vs 18）。

## A3　`口蘑` 无来源行，却挂在 `mogu` 别名上

> 由 A2 第 ② 项同批裁决产生（2026-09-18）。**这条记的是一个「已留痕的未决状态」**：
> 别名保留，但依据等级明确写成「项目专业判断」而非来源行 —— 不允许「无证据却挂在别名上」
> 这个状态**无声地**存在。

| | |
|---|---|
| **现状** | `core/data/food_properties.json` 的 `mogu`（蘑菇）条目 `aliases` 与 `keywords` 两处都含 **`口蘑`**。而 `docs/food-properties-sources.json` 里 `mogu` 的两条 `evidence`（蘑菰、香蕈）**都不指向口蘑**，全表 47 条也没有任何一处提到它 |
| **为什么不删** | A2 的裁决是 (C)：金针菇/杏鲍菇/平菇 3 个**物种**移出，但 `口蘑` 在通行饮食语境里就是「蘑菇」的**口语同义语** —— 删了用户说「口蘑」只能落到模型推测分支。保留同义匹配有价值 |
| **为什么不直接登记来源** | 依据必须**真实存在**。50 号文件（外部语料，文件名 50-食物四气五味.md，**不在本仓库**）的 §5.3 别名索引里到底有没有「口蘑」行，**无法在本地核实**；没有行号与原文就只能编 —— 那正是来源登记表 `hard_rules` 第 1 条禁止的（「不得用通行表述伪装权威依据」）。**也没有「借蘑菰的行号」这条路**：蘑菰（纲目 23487）是另一个物种的证据 |
| **当前处置** | ① `口蘑` 保留在 `mogu` 的 `aliases` + `keywords`；② `sources.json` 的 `mogu.mapping_review.note` 与数据的 `mogu.review_note` 都写明「依据＝项目专业判断，非来源行」；③ 本条即留痕处 |
| **需要什么** | 在**能读到 50 号文件**的环境里查 §5.3 索引，二选一：<br>**(α)** 确有「口蘑」行（或语料里口蘑是独立条目）→ 补 `locator` + `verbatim` 并登记，本条关闭；<br>**(β)** 无该行、且判为**不同物**（如蒙古口蘑一系）→ 从 `aliases` + `keywords` **两处**移出（与 A2 的 3 味菇同法）；<br>若判为**通行同义语**则维持现状，把本条状态改为「已确认（无来源、凭项目判断保留）」 |
| **验收标准** | 三种结论之一被明确选定并落到数据与文档；「无证据却挂在别名上」这一状态不再**无声**存在（要么有行号，要么写明无行号、是项目判断） |
| **相关文件** | `core/data/food_properties.json`（`mogu`）、`docs/food-properties-sources.json`（`mogu.mapping_review.note`）、`docs/food-properties-batch2-confirm.md` §1.3、`core/app/services/food_lookup.py`（匹配扫描把 `aliases` 与 `keywords` 合并，见 `_find_entry` 第 2 级） |

---

# B 类 · 等 nanple

## B1　7 处药典不一致待复核

| | |
|---|---|
| **现状** | 《中国药典》2020 年版一部与项目 `herbs.json` 有 **7 处不一致**（44 味中一致 36、未收载 1） |
| **决定** | **2026-09-17 已定：先保留项目值，不改数据，等 nanple 复核后再决定是否改** |
| **差异影响** | 已逐条做过**代码取证**，结论是**这 7 处都不改变任何确定性判定结果**（饮片的四气/五味/归经不参与任何护栏或规则运算，唯一实质消费者是 Agent2 的候选描述提示词）。详见 `docs/herb-nature-crosscheck.md` §3.1/§3.2 |
| **需要什么** | nanple 判断每一处以哪个为准（药典 or 项目），或说明项目值的其他依据 |
| **7 处明细** | 桑椹（凉↔寒）、百合（凉↔寒）、淡竹叶（凉↔寒）、白扁豆（平↔微温）、佛手（缺「酸」、缺「胃」经）、红枣（缺「心」经）、荷叶（多「淡」） |
| **验收标准** | nanple 给出逐条结论；若需改数据，改完运行 `python scripts/build_herb_crosscheck.py --refresh` 重建参考数据，并同步更新 `core/tests/test_herb_crosscheck.py` 的 `EXPECTED_INCONSISTENT` 清单（测试会提示） |
| **相关文件** | `docs/herb-nature-crosscheck.md`、`core/data/herb_nature_reference.json`、`core/scripts/build_herb_crosscheck.py` |

## B2　问卷项目 `phlegm_dampness` 拼写不一致

| | |
|---|---|
| **现状** | ✅ **已解决（2026-09-18）。** 原状：`tcm-constitution-questionnaire/` 用 `phlegm_dampness` 表示痰湿质；本项目 `core/data/constitution.json` 与 `enums.py` 用 `phlegm_damp`。9 个体质 id 里**只有这 1 个**不同 |
| **影响（原）** | 两边的体质标识无法直接对接。**这也独立证明了 `tcm-constitution-questionnaire/` 当时尚未接入本项目**——如果接过，这个不一致早在跑测试时就暴露了 |
| **决定** | **2026-09-17 已定：作为第一个 Issue 提给 nanple。** 2026-09-18 由项目侧自行解决：二选一里**选 ①（问卷侧改名）**，因为项目侧的 id 已在 9 型里全部确认（见 B3），是承重的那一套 |
| **解决方式** | **方案① 问卷侧改名（2026-09-18 已执行）**：把仓库内 `tcm-constitution-questionnaire/` 的 `phlegm_dampness` 全部改为 `phlegm_damp`，共 **3 文件 8 处**（`tcm-constitution-questionnaire/tcm_constitution/questions.py` 4、`tcm-constitution-questionnaire/questionnaire.csv` 3、`tcm-constitution-questionnaire/tests/test_scoring.py` 1）；`core/` **一个字未动**。方案②（API 边界加 `CONSTITUTION_ID_ALIASES` 适配映射）**不采纳** —— 它会让两种标识串长期并存，漏用一处即静默失效。完整决策过程、取证与改动清单见 **`docs/b2-constitution-id-alignment.md`** |
| **验收标准** | ✅ **已达成**：两边对同一体质给出同一个标识串 `phlegm_damp`；`core/` 无需任何适配层即可直接用问卷输出的体质 id。防回潮由 `core/tests/test_questionnaire_id_alignment.py`（4 守卫 × 正负控 = 8 条）守住 |
| **相关文件** | `docs/b2-constitution-id-alignment.md`（方案与执行记录）、`tcm-constitution-questionnaire/tcm_constitution/questions.py`、`tcm-constitution-questionnaire/questionnaire.csv`、`core/tests/test_questionnaire_id_alignment.py`（守卫）、`core/app/domain/enums.py`（事实源，未改） |
| **仍未解决** | ⚠️ **标识对齐已解决，接入分阶段**：**阶段 1（链路能力）与阶段 2（终端接线）已于 2026-09-18 落地** —— 换算层 `core/app/domain/constitution_resolver.py`、四处接入点同步、跨路径一致性守卫，终端 `--questionnaire` / `:qz`。**阶段 3（网页版）本次不做（D5′ ①）**：`ui/web/index.html` 是纯静态 JS，跑不了 Python 子包，拿不到问卷的 `scores`；等 `/api/analyze-offline` 那一轮一起定端点。方案与执行记录见 `docs/b2-questionnaire-integration-plan.md`（7 个决策点）与 `docs/b2-integration-execution-plan.md`（落地步骤） |

## B3　244 个配伍判定（进度口径见本节「附注」）

| | |
|---|---|
| **现状** | ⚠️ **本项目有两个并存的账本分母，不能混算**：<br>**244 = 四型账本** —— 34 味 × 4 型 `suitable_constitutions`（136）+ 27 味 × 4 型 `unsuitable_for`（108），**只覆盖 4 个新体质**（阴虚／血瘀／气郁／特禀），本节的所有格数都指它；<br>**221 = 九型账本** —— 136（契约内四型）+ 85 扩展层（湿热 34 + 平和 34 + 气虚/阳虚/痰湿 17），是 2026-09-20 那批 34 味 × 9 型判定的口径，记在 `herbs.json` 的 `_meta.constitution_extension.evidence_batch`。<br>⇒ 两者的四型部分同为 136，但 221 那 85 格扩展层**在 244 里没有对应分母**，所以本批补的格子不能直接拿 221 去减 227 —— 本批 7 处动作里只有 2 格同时落在两个账本的四型部分（5.4／5.5），红枣 × 痰湿与紫苏 × 气虚**只属于 221**。<br><br>另：**244 这个分母本身不随数据更新**——它的算式含「27 味可标 `unsuitable_for`」，而实测有该字段的味已增至 **29**（按现算式分母会是 252）。不改，因为它是一个**历史承诺**（`handover.md` §6 决策 27 与历次批次都按它记账），改了 244 就从「账本」变成「另一个账本」，历次批次的加减全部失效。<br><br>**Step 1 + Step 2 均已完成（2026-09-17）**：枚举 / `constitution.json` / 就绪闸门已扩到 9 型；`herbs.json` 写入 10 格「公开来源直接点名」的配伍标注，**九型已全部就绪并对外可见**。**余下的 244 格是完备度问题，不再是功能阻塞**：4 个新体质逐味判定的完整覆盖约为 34 味 × `suitable_constitutions` + 27 味 × `unsuitable_for`，目前只填了 **17** 格（10 + 第二批 5 + 第三批 2）。<br>同日另有 **cautions 补写批次**：把项目**自有** `cautions` 已明写阴虚不宜的 10 味追加进 `unsuitable_for`（见「附注」），属项目数据一致性修复，不引用外部来源、不含外推。<br>**同日第二批（5 格）**：由外部专业意见裁定 —— 3 格走 `cautions` 软通道（荷叶×阴虚、山楂×阴虚、槐花×血瘀）、1 格硬屏蔽（香薷×阴虚，`evidence`）、1 格升位（山楂×气郁 → `suitable_constitutions`）。见 `docs/herbs-9types-batch2.md`。<br>**第三批（2026-09-20，34 味 × 9 型判定回填，提交 `b3cde6e`）**：由外部专业意见（TCM.Skill）逐格判定，口径与依据等级见 `herbs.json` 的 `_meta.constitution_extension.evidence_batch`（`_meta.version` 0.5.0 → **0.6.0**）。**7 处动作**：5.4 酸枣仁 × 阴虚、5.5 山药 × 阴虚 → 写进 `suitable_constitutions`；5.2 红枣 × 痰湿 → 改判 `caution_only`（从 `unsuitable_for` 移出 `phlegm_damp`，`cautions[0]` 原样保留）；5.3 香薷 × 阴虚 → 由 `evidence` **降为 `inference`**（七版:671 只记「表虚有汗及暑热证」，未及阴虚），数据字段不动；5.10 紫苏 × 气虚 → 降为 `caution_only`，`cautions` 末尾追加「气虚多汗者不宜食用」；5.1 山楂 × 阴虚维持 `caution_only`、5.6 麦冬 × 阴虚**判定采纳但不入数组**（→ **E7**）、`balanced` 24 味**零动作**（选① 不清空，避免阻断服务）。<br>⚠️ **只有 5.4 与 5.5 两格落在 244 账本内并改变落地数**（15 → 17）：5.2 与 5.10 落在**扩展层体质**（痰湿／气虚），不在这 244 格的分母里；5.3 只是依据等级降级、5.6 刻意不入数组、5.1 零变更，都不改变落地数 |
| **为何只有人能解** | **GB/T 46939-2025 只规定体质分类、问卷、计分与判定阈值，不提供任何饮食或饮片建议。** 余下的格子无标准可依 |
| **写入口径** | 只写「公开来源**直接点名**该饮片适用于/不适用于该体质」的组合（共 10 格）；药典功效外推、项目 `cautions` 推导**一律不写**，留待人工审核。逐条依据与推导过程见 `docs/herbs-9types-draft.md`。**空缺 ≠ 安全或不宜，空缺只表示「未判定」**。<br>⚠️ 第二批（5 格）口径**不同**：依据是**外部专业意见**（不是公开来源点名），其中 1 格 `evidence`、4 格 `inference`；「依据等级决定写哪个字段」由 `test_safety.py` 强制，见 `docs/herbs-9types-batch2.md` |
| **防错（已实战检验）** | `filter_by_constitution` 在「该体质一条数据都没有」时**抛错而非静默退化**（静默退化会让阴虚质拿到温补辛温之品，方向相反且从输出表面看不出来）。这个机制在 Step 1→Step 2 之间被真实验证过：当时 4 型枚举有了、数据没有，被两层闸门稳稳挡住（`/api/constitutions` 不返回；直接指定则 **422 `CONSTITUTION_NOT_READY`**，不静默回落平和质）。Step 2 之后闸门仍在，守的是**将来**新增而未备数据的体质 |
| **需要什么** | nanple（或另一位懂中医的人）给出余下的判定。原「5 处已单列待确认」（山楂×阴虚、山楂×气郁、槐花×血瘀、香薷×阴虚、荷叶×阴虚）**已于 2026-09-17 由外部专业意见裁定并写入**（见 `docs/herbs-9types-batch2.md`）；其中 ⚠️ **山楂×气郁** 是 `inference` 级「倾向」判定，写入后**升位到气郁候选首位**（山楂的 catalog 下标早于红枣、薄荷），疑虑已记入 `herbs.json` 的 `rulings.priority_review`，**请优先复核** |
| **验收标准** | ① ✅ **已达成（2026-09-17）**：`ready_constitutions()` 返回全部 9 型 —— 等价于 `GET /api/constitutions` 返回 9 项、四个新体质已出现在前端下拉框；<br>② ⚠️ **`MissingConstitutionDataError` 不是要消除的目标**，它是设计行为：体质无数据时**必须**抛错，所以测试里**应当保留**主动构造该场景的用例（`test_safety.py`、`test_constitution_readiness.py`）。本条原写作「`pytest` 不再触发 `MissingConstitutionDataError`」，方向反了，且不可判定——没人去测那 4 型时它同样不触发；<br>③ **余项（完备度）**：余下格子经人工判定后可继续补写，**补写不需要改代码**——数据一多，候选集的排序与方向收敛自动变好 |
| **相关文件** | `docs/constitution-9-types.md`（九型资料 + B 栏饮食方向）、`docs/herbs-9types-draft.md`（逐条依据草稿）、`core/data/herbs.json`、`core/data/constitution.json` |
| **附注** | 已写入的 10 格（公开来源点名）：**宜**——桑椹×阴虚，山楂/红枣/生姜×血瘀，红枣/薄荷×气郁，红枣/山药×特禀；**忌**——乌梅×血瘀、乌梅×气郁。**另有 cautions 批次 10 味**（项目自有数据，属推导非权威依据）：茯苓、陈皮、龙眼肉、生姜、薏苡仁、茉莉花、橘红、藿香、紫苏、佛手 → 阴虚 `unsuitable_for`。<br>**第二批 5 格**（外部专业意见）：香薷×阴虚（**忌**，`evidence` → `unsuitable_for`）、荷叶×阴虚（慎用，`inference` → `cautions`）、山楂×阴虚（慎用，`inference` → `cautions`）、山楂×气郁（宜（倾向），`inference` → `suitable_constitutions`）、槐花×血瘀（慎用（低置信），`inference` → `cautions`）。按 A1 口径，所有写入都属**未人工审核数据**，界面仍标「待验证」。<br>**第三批（2026-09-20，7 处动作）**：酸枣仁 × 阴虚（**宜**，`E′` → `suitable_constitutions`）、山药 × 阴虚（**宜**，`E′` → `suitable_constitutions`）、红枣 × 痰湿（改判**慎用**，`unsuitable_for` → `cautions` 软通道）、香薷 × 阴虚（`evidence` **降为** `inference`，仍在 `unsuitable_for`）、紫苏 × 气虚（**慎用**，`inference` → `cautions`）、山楂 × 阴虚（维持**慎用**，`caution_only`）、麦冬 × 阴虚（**宜**，`E` 级，但**只记录不入数组** → **E7**）。<br>⚠️ **两处口径提醒**：① 账本数按「判定动作」计，故第二批 3 格 △（只进 `cautions`）算已落地、而同日的 cautions 补写批次 10 味（真的进了 `unsuitable_for`）不算 —— 若按数组实测则为 **29 / 244**（NEW4 suitable 14 + unsuitable 15），余 **215**。两个数都写在这里，免得后来人照数据一数以为文档错了；② 原「余 239」是 `244 − 5`，漏减了第一批 10 格，本次按 `244 − 17` 修正为 **227** |

---

# C 类 · 需要先拿到外部资料

## C1　《中华本草》列全空

| | |
|---|---|
| **现状** | `docs/herb-nature-crosscheck.md` 的《中华本草》列**一格都没填**，全部标注「待确认」 |
| **原因** | **取不到公开数据，不是遗漏。** 实测记录（完整版在 `core/data/herb_nature_reference.json` 的 `_meta.zhonghua_bencao_status`）：<br>① 药智网「中华本草数据库」需登录<br>② tcmdoc.cn 页面标题就是「中国药典、中药大辞典、中华本草、全国中草药汇编」，但返回 **403**<br>③ 中医世家 zysj.com.cn **拒绝连接**<br>④ wiki8.cn **403**、zhongyoo.com **521**<br>⑤ yao86.com 虽在【参考文献】里引用《中华本草》，但它展示的【药性】是**药典**的值 —— 不能当《中华本草》的来源用 |
| **决定** | **2026-09-17 已定：统一标「待确认」，不猜。** 按项目所有者的判断，不值得为它拉长工期——药典是官方标准，比《中华本草》更合规、也更可抓取 |
| **⚠️ 关键提醒** | **空缺 = 未核实，不等于与药典相同。** 《中华本草》与《中国药典》对同一味药的性味归经多数相同但并非全部相同（药典是药品标准，会随版次修订）。任何人引用这份文档时都不能把空缺读成「一致」 |
| **需要什么** | 《中华本草》（上海科学技术出版社，国家中医药管理局《中华本草》编委会）的电子版或复印件 |
| **拿到后怎么做** | 把数据给我/开发，补进 `herb_nature_reference.json` 的每条记录，新增一个 `zhonghua_bencao` 字段并与药典列逐条比对（脚本已按「参考列」结构预留） |
| **相关文件** | `docs/herb-nature-crosscheck.md` §6、`core/data/herb_nature_reference.json` |

## C2　国标 A 栏是转引，非标准正文

| | |
|---|---|
| **现状** | `docs/constitution-9-types.md` 的 A 栏（九型特征）**全部是官方解读/权威媒体的转引**，每一条都标注了来源与「非标准正文」 |
| **原因** | **标准正文取不到。** 已实测：<br>① 国家标准全文公开系统的「在线预览」在非浏览器环境只返回「当前浏览器暂不支持标准全文在线预览服务」回退页<br>② 同页「下载标准」走同一条在线预览链路，未暴露可直接下载的 PDF 地址<br>③ 中国标准出版社在人人文库上传的正版 PDF（20 页 / 295 KB）需 44 积分，无文本预览 |
| **决定** | **2026-09-17 已定：用官方解读/起草人访谈转引，逐条标注「非标准正文，系转引」**，不冒充原文 |
| **当前 A 栏来源** | 科技日报 2026-02-10 专访王琦院士（标准主要起草人）为主；辅以央广网、广东省中医药局 |
| **需要什么** | 用 Chrome/Edge 打开 [国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=827D6D266BA52A2983F93638DA871028) 的在线预览，或取得中国标准出版社正版 PDF |
| **拿到后怎么做** | 把 PDF 给我/开发，逐条替换 A 栏为原文并标注条款号；同时可核对「判定阈值 40 分」的口径（该条也是转引，见 `docs/constitution-9-types.md` §7） |
| **相关文件** | `docs/constitution-9-types.md` §1.1、`docs/constitution-9-types.json` 的 `standard.fulltext_status` |

## C3　引用的药典是 2020 版，已被 2025 版废止

> 2026-09-18 落地 E3（饮片侧逐味公开依据链）时才浮现的事实。以前药典只是核对表里的一列，
> 现在它是**直接呈现给用户的公开依据** —— 依据一旦对用户可见，版本就得如实标。

| | |
|---|---|
| **现状** | 饮片侧两份资料（`core/data/herb_nature_reference.json` 的逐条对照、`core/data/herb_evidence_sources.json` 的属性依据）引用的都是**《中国药典》2020 年版一部**。而 2020 年版**已被 2025 年版废止**（2025 年第 29 号公告，2025-10-01 施行）|
| **为什么不能不管** | 「2020 版」这个版本号现在会随推荐一起出现在终端与网页的「公开依据」里。标一个已废止的版次而不说明，等于给用户一个过期的出处 |
| **当前怎么处理的** | **如实留痕，不猜。** `herb_evidence_sources.json` 的 `_meta.source_registry.chp2020.caveat` 写明「版本为 2020 版，已被 2025 版废止；2025 版在线全文尚未取得，跨版性味归经是否变动未经核实」，`EvidenceReference.note` 会把这句话带进两个壳 |
| **⚠️ 未经核实的假设** | 「跨版性味归经变动概率不高」**是假设，不是结论** —— 药典是药品标准，会随版次修订（同一判断在 `core/data/herb_nature_reference.json` 的 `_meta.zhonghua_bencao_status` 里也写过）。换版之前，不得把「与 2020 版一致」读成「与现行版一致」 |
| **影响面** | **不改变任何确定性判定**：饮片的四气/五味/归经不参与护栏与规则运算（取证见 B1 的「差异影响」）。它阻塞的是**对外呈现的准确性**，不是功能 |
| **需要什么** | 《中国药典》2025 年版一部的在线全文或可用的官方检索接口。国家药典委员会「中国药典在线版」目前提供的是 2020 版（查询参数 `bookId=1`）|
| **拿到后怎么做** | ① 先确认 2025 版对应的 `bookId`（或新的检索接口）；② 沿用 `core/scripts/build_herb_crosscheck.py` 的同一套逻辑重建对照数据，四气降档映射复用 `_meta.mapping_rules.nature`、**不新造**；③ 逐条比对跨版差异 —— 若确无变动，就把 `caveat` 改成「已核对 2025 版，与 2020 版一致」并保留一条核对记录；④ 运行 `core/scripts/build_herb_sources.py --write` 重建登记表与 `docs/herb-evidence-sources.md` |
| **验收标准** | 引用版本从 2020 改为 2025（或明写「已核对 2025 版」），`chp2020` 的 id 与 `caveat` 同步更新；`pytest` 全绿 |
| **相关文件** | `core/data/herb_nature_reference.json`、`core/data/herb_evidence_sources.json`、`docs/herb-evidence-sources.md`、`core/scripts/build_herb_crosscheck.py`、`core/scripts/build_herb_sources.py` |

---

# D 类 · 等决定或等实施

## D1　玫瑰花/茉莉花的代码改动

| | |
|---|---|
| **现状** | 两个决定**已登记但代码未动**，状态为 `待实施（等 nanple 审数据）` |
| **决定内容** | **玫瑰花**：保留，但名称需明确品种为「玫瑰花（重瓣红玫瑰）」<br>**茉莉花**：标注「仅作参考」，推荐时排除 |
| **为何没直接做** | 都有真实技术约束：<br>① **改名会打断白名单匹配** —— `herb_by_name()` 是 `check_blend` / `check_constitution_fit` / `filter_by_constitution` 唯一的名称索引，`if name not in catalog: blocked`。改名而不同时加别名，Agent2 输出的「玫瑰花」会被判成白名单外饮片**直接拦掉**，这是行为回归<br>② **「推荐时排除」需要新增能力** —— 现有 `exclude_herbs` 是**每请求的用户自选排除**，不是目录级开关 |
| **⚠️ 必须知道的限制** | 品种约束**代码表达得了、核实不了**。「原料确为重瓣红玫瑰」是采购/供应链要求，代码只能把它写在数据里。按项目所有者自己定的规则（做不到这个约束就标「仅作参考」），若无法保证原料品种，玫瑰花应降级为「仅作参考」 |
| **实现方案（已备好）** | `models.py` 给 `CatalogHerb` 加两个可选字段（`aliases` / `reference_only`）+ `safety.py` 的名称索引加别名 + `filter_by_constitution` 与 `matcher.fallback_recommend` 过滤 `reference_only` + 测试 |
| **风险提示** | 不改的后果是**「文档说改了、代码没改」**——正是这个项目踩过的坑，所以状态明确标成「待实施」而不是「已实施」 |
| **验收标准** | 决定状态从「待实施」变为「已实施」，且 `python scripts/check_herb_catalog.py --format md --out ../docs/catalog-compliance.md` 重新生成后文档同步 |
| **相关文件** | `core/scripts/check_herb_catalog.py` 的 `DECISIONS` 常量、`docs/catalog-compliance.md` §处置决定 |

## D2　本地提交未推送

| | |
|---|---|
| **现状** | ✅ **已解决。** 2026-09-17 推送 `010db2f..7ed495a`（6 个提交）到 `origin/main` |
| **验收结果** | `git log --oneline origin/main..HEAD` 输出为空；`git ls-remote origin refs/heads/main` 返回 `7ed495a7be…` |
| **影响** | nanple 现在能在 GitHub 上看到这批产出（食药物质目录、九型资料、药典核对、四项决定、交接文档） |
| **约定** | 此后的推送仍由项目所有者决定，**不属于开发默认动作** |

**解决于** 2026-09-17，推送范围 `010db2f..7ed495a`。

推送前的安全检查（5 项）全部通过：工作区干净；6 个待推送提交；`.env`/`credentials.env`/`dsh-home/`
均为误跟踪且历史上从未被 `add`；31 个提交的全历史扫描只命中测试哨兵常量
`sk-SENTINEL-DO-NOT-LEAK-…`（13 处，全在 `core/tests/test_key_handling.py`）。

## D3　7 味须预处理（捣碎/掰开）未纳入本轮

> 2026-09-18 做「煎煮 vs 冲泡」缺口时一并扫出来的**第三类**问题。本轮不做，
> 但已定「单列挂起」—— 免得埋在扫描稿里被忘掉。

| | |
|---|---|
| **现状** | 有 7 味的 `brewing` 明写「不捣碎/不掰开就不出味」，而它们**既不属须煎煮、也不属后下**：`决明子`「整粒几乎不出味，务必捣碎」、`酸枣仁`「整粒不出味」、`红枣`「整颗冲泡不出味，务必掰开或切片」、`乌梅`「可略捣」、`栀子`「可略捣碎」、`鸡内金`「研粉冲服」、`白扁豆`「炒后捣碎更易出味」（白扁豆同时是须煎煮味） |
| **影响** | **无安全性影响**（不像煎煮那样「出不了味却照样端给用户」），只影响出味效果与口感 |
| **为何本轮不做** | ① 「须预处理」的判定颗粒度比「须煎煮」主观得多（「可略捣」算不算须？）；② 处置方式也不同 —— 煎煮是**换冲泡方式**，预处理是**换采购形态或加一步操作**，属新一类，先不混进本轮机制 |
| **需要什么** | 定两件事：a) 判定口径（只有「务必/须」算，还是「可略」也算）；b) 处置方式（进候选清单提示／加数据标记／只写进采购规范） |
| **相关文件** | `core/data/herbs.json` 的 `brewing`、`docs/agent2-9types-brew-plan.md` §2.2 附类 C |

## D4　「白酒」的 `keywords` 混入「黄酒」

> 2026-09-19 做 B2（主食/制品 12 条来源登记）时扫出来的**既有**问题 —— B2 的边界是
> 「只登记来源、不动数据表」，故只留痕不改。它与 **A2 ②**（金针菇/杏鲍菇/平菇 从
> `aliases` + `keywords` 移出）**同族**：都是「一个词挂在别的条目上，于是以 0.9 置信度
> 呈现一个不属于它的四气」。

| | |
|---|---|
| **现状** | `core/data/food_properties.json` 的 `baijiu`（白酒，`nature: hot`）`keywords` 为 `["白酒", "黄酒"]`，`aliases` 为空。「黄酒」于是被 `_find_entry` 匹配到白酒条目，直接拿到 `hot` |
| **为什么算问题** | 黄酒与白酒是**两种不同的酒**，四气不同档：B2 给白酒登记的参考是《本草纲目》行 20645「烧酒」条下的【气味】行 20649（**大热**）；而《中医饮食营养学》行 1721 的「酒」记**温** —— 差一档。用户说「黄酒」会拿到白酒的 `hot`，且因属「表内命中」，界面按 **0.9** 呈现并标「已收录」 |
| **当前处置** | 本批**不改数据**（B2 只登记来源）。本条即留痕处；发现记录在 `docs/food-properties-b2-staple.md` §10 第 1 条与 §12.5 第 2 条 |
| **需要什么** | 定一件事，二选一：<br>**(α) 从 `keywords` 移出「黄酒」**（与 A2 ② 同法）—— 此后用户说「黄酒」走「表未覆盖」的推测分支（0.3，界面标「食性未经验证」）；<br>**(β) 另立 `huangjiu` 条目** —— 须先有可引用的来源行（语料里「酒」记温，强度与可映射性要先评估，不能拿白酒的依据顶）。<br>两者都要改数据，故等所有者拍板 |
| **验收标准** | 二选一被明确选定并落到数据；「一个词挂在不属于它的条目上」这一状态不再**无声**地存在 |
| **相关文件** | `core/data/food_properties.json`（`baijiu`）、`core/app/services/food_lookup.py`（`_find_entry` 第 2 级把 `keywords` 与 `aliases` **合并扫描**，只删一处无效）、`docs/food-properties-sources.json`（`baijiu` 条目的登记依据）、`docs/food-properties-b2-staple.md`（发现与留痕） |

## D5　「糖」的 `aliases`/`keywords` 混入「红糖」，而红糖应另立条目

> 2026-09-19 做 B3（糖/腐竹/毛豆/樱桃/葱 五条来源登记）时确认。与 **D4**（白酒混入黄酒）
> **同族**：一个词挂在别的条目上，于是以 0.9 置信度呈现一个不属于它的四气。本条比 D4 更进
> 一层——**红糖与白糖的四气不同档**（温 vs 平）。

| | |
|---|---|
| **现状** | `core/data/food_properties.json` 的 `tang`（糖，`nature: neutral`）`aliases` = `["白糖","砂糖","红糖"]`、`keywords` = `["糖","白糖","红糖"]`。全表**没有** `hongtang` 条目 |
| **为什么算问题** | 权威口径把两者**分开**：中国中医药报网《食糖补虚 亦应有度》（2026-05-27）逐条写「白糖味甘、**性平**」「红糖味甘**性温**，归肝、脾、胃经」「饴糖味甘**性温**」；国家中医药管理局名词术语项目审核认证词条「白沙糖」记「味甘，性平」。⇒ 用户说「红糖」时，系统给的是糖条的 `neutral`，与权威口径的「温」**差一档**，且界面按「表内命中」以 **0.9** 呈现 |
| **当前处置** | B3 只登记「糖 → 白沙糖」的来源（判 ① 一致），**不动数据**。本条即留痕处；发现记录见 `docs/food-properties-b3-network-sources.md` |
| **需要什么** | 两步，**都要改 `core/data/`**：<br>**(α)** 新建 `hongtang` 条目（`name: 红糖`、`nature: warm`、`flavors: ["sweet"]`，并为其登记来源）；<br>**(β)** 把「红糖」从 `tang` 的 `aliases` **与** `keywords` **两处**移出（`_find_entry` 第 2 级把两处合并扫描，只删一处无效）。<br>顺序不能颠倒：先移出会让「红糖」落到 0.3 的推测分支（比现在还差），先建条则一次到位 |
| **验收标准** | ① 表里有 `hongtang` 条目且 `nature = warm`；② 「红糖」不再挂在任何其他条目的 `aliases`/`keywords` 上；③ 走 `patch_food_table.py` 的正规通道，不手改 `food_properties.json` |
| **相关文件** | `core/data/food_properties.json`（`tang` / 待新增的 `hongtang`）、`core/app/services/food_lookup.py`（`_find_entry` 第 2 级把 `keywords` 与 `aliases` 合并扫描）、`core/scripts/patch_food_table.py`（数据改动唯一通道）、`docs/food-properties-b3-network-sources.md` |

## D6　「葱」是否拆成葱白 / 葱叶

> 2026-09-19 做 B3 时浮现。B3 按**葱白**给 `cong` 登记依据（判 ② 口径分歧），
> 拆不拆留待本条裁定 —— 免得「部位方向不同」这件事只靠一条 `open_question` 承载。

| | |
|---|---|
| **现状** | `cong`（葱，`nature: warm`、`aliases = ["大葱","小葱","香葱"]`、`keywords = ["葱"]`）是**一条**条目，没有部位维度 |
| **为什么值得考虑** | 两处依据对**部位**的判法不一致：<br>《本草纲目》行 20838（50 号文件 §4.9）：「葱茎白【气味】辛，**平**。叶∶**温**。根须∶平。」——且同行引陶弘景「葱有寒热，**白冷青热**」，方向与下条相反；<br>国家中医药管理局名词术语项目审核认证词条「葱」（中国医药信息查询平台）：「葱白：味辛，**性温**；归肺、胃经；葱叶：味辛，**性温**；葱须：味辛，**性平**」。<br>⇒ **葱白一档之差**（古籍「平」vs 官方审核词条「温」）；葱叶两处都判温；葱须两处都判平 |
| **当前处置** | B3 按**葱白**登记，`cong` **维持 `warm`**，以 `open_question`「按葱白走」留痕，**不拆条** |
| **需要什么** | 三选一：<br>**(α) 维持单条目** —— 承认「葱」泛指可食部位，以葱白为代表取值 `warm`，「葱叶」「葱花」仍命中同一条目；<br>**(β) 拆成 `congbai`（温）+ `congye`** —— 叶取温还是平取决于采哪一源，需先定源；<br>**(γ) 只把「葱叶」类词从 `aliases`/`keywords` 移出** —— 与 A2 ② 同法，但会牺牲「葱叶」的可用性 |
| **验收标准** | 三选一被明确选定并落到数据与来源登记；「部位方向不同」这一事实不再只靠一条 `open_question` 承载 |
| **相关文件** | `core/data/food_properties.json`（`cong`）、`docs/food-properties-sources.json`（`cong` 条目的登记依据）、`docs/food-properties-b3-network-sources.md` |

## D7　派生规则缺「温度 / 介质」两个变量

> 2026-09-19 做 B4（派生规则由 3 条改 6 条）时确认：同学的规则覆盖了「基底 + 加工 + 深度 +
> 发酵 + 合成 + 冲突」，但另外两个变量**明确未纳入**。留痕，免得被当成「已经考虑过了」。

| | |
|---|---|
| **现状** | ① **温度**：同一成品在不同温度下的偏性，现由**温度前缀层**单独处理（`nature_math` 的 `CHILL_PREFIXES` / `HEAT_PREFIXES`，命中即 ±1），**不属派生规则**；② **介质**：同一食材在不同介质/汤底里的偏性（如「同是面条，清汤 vs 红油」）**完全没有实现，也没有字段承载** |
| **为什么值得单列** | 「做法」改的是**食材本身**，「温度/介质」改的是**呈现与搭配** —— 两者不是同一类变量。B4 的规则若被当成「成品偏性的完整算法」，就会漏掉这一整类 |
| **当前处置** | B4 **只做六条规则**，本项只留痕（见 `docs/food-properties-b4-derivation-plan.md` §9 拍板点 A）|
| **需要什么** | 定两件事：**(α) 温度**是否**只**保留现有 ±1 前缀机制、不再扩档（若扩档，与 B4 的 R3 加工深度会重叠）；**(β) 介质**是否作为独立维度纳入 —— 若是，须先定它的取值域（清汤/红油/奶/酒/醋…）与是否有来源可依 |
| **验收标准** | 两者各自被明确裁定「纳入 / 不纳入」；若「不纳入」，B4 的规则文档里要有**一句显式排除**（现在只在挂起项里） |
| **相关文件** | `docs/food-properties-b4-derivation-plan.md`、`core/app/domain/nature_math.py` |

## D8　派生规则缺「食用场景」维度

> 同上批确认。与既有的一条场景规则相邻，故单列。

| | |
|---|---|
| **现状** | B4 的派生规则按「**成品本身**」定值，不考虑**食用场景**（空腹 / 夜宵 / 配酒 / 运动后…）。项目里**已有一条场景规则**：`matcher.py` 的 `RULES` 中 `late_night`（陈皮 4 + 茯苓 6，`match_natures` 只含 `Nature.UNKNOWN`，`keywords` 为空）—— 即**只在整餐四气判不出时**才计分 |
| **为什么值得单列** | 「场景影响偏性」与 B4 是同族问题，但**分属两层**：`late_night` 在**离线场景规则层**（决定推荐搭配），B4 在**数据/规则层**（决定单条食物的四气）。将来若要扩场景，两处口径需一致 |
| **当前处置** | B4 不做，只留痕 |
| **需要什么** | 定是否纳入、纳哪些场景。⚠️ 若纳入，须同时给 `late_night` 补测试 —— 它**目前无测试覆盖**，且「只在 `UNKNOWN` 时计分」这个前提很容易被改动无声破坏 |
| **验收标准** | 场景维度的取舍被明确裁定；若维持现状，`late_night` 的「仅 `UNKNOWN` 计分」应有测试固化 |
| **相关文件** | `docs/food-properties-b4-derivation-plan.md`、`core/app/services/matcher.py` |

## D9　反向查询未做（按四气反查食物）

| | |
|---|---|
| **现状** | 现有链路是**单向**的：「食物 → 四气」（`resolve_food`）。「给我几样寒凉的」这类**反查**没有入口，也没有任何函数在做（表内无反向索引）|
| **为什么不是顺手能做** | ① 要跨两张表（食物 147 条 + 饮片 44 味），两边的字段结构不同；② 必须处理**置信度过滤** —— 0.3（模型推测）/0.1（判不出）的条目**不该**被反查出来当结果，否则等于把「未验证」当「已验证」用；③ 还要定排序口径（按四气强度排，还是叠加体质适配排）|
| **当前处置** | 未做。由 B4 的规则讨论带出（有了「可算」的四气，反查才第一次成为有意义的功能）|
| **需要什么** | 定是否做、做到哪个壳（终端 / 网页），以及置信度门槛 |
| **验收标准** | 若做：反查结果**不得**含 `pending`/`unresolved` 来源的条目（或必须显式标注）；若不做：本条明确关闭 |
| **相关文件** | `core/data/food_properties.json`、`core/app/services/food_lookup.py` |

## D10　用户反馈闭环未做

| | |
|---|---|
| **现状** | 用户拿到判定/推荐后若认为不可信，**没有任何落点**：食物表里既无反馈字段，也无上报通道。A1 的审核进展现**只靠内部 `review_status` 标记**（且全是 `pending`）|
| **为什么值得单列** | 它决定 A1 的 147 条审核将来是**只靠审核人逐条读**，还是能拿到真实世界的输入。也是「未经验证」标注能否收敛的唯一外部信号 |
| **当前处置** | 未做。由 B4 带出（B4 要给条目写「倾向性判断 + 建议观察」，而**观察结果无处回收**）|
| **需要什么** | 定闭环形态三选一：**(α) 本地留痕**（写进用户本地文件，不上报）；**(β) 上报汇总**（需定隐私边界）；**(γ) 只做统计**（不绑定具体条目）。⚠️ 注意食物侧**现无 `cautions` 字段、也无任何反馈字段**，任何形态都要先定字段或存储位置 |
| **验收标准** | 形态被明确选定；若不做，本条明确关闭（否则 B4 那些「建议观察」的措辞就是**单向的**）|
| **相关文件** | `docs/food-properties-review-sheet.md`、`core/data/food_properties.json` |

## D11　「寿司 / 咖喱」是否按子类型拆条

| | |
|---|---|
| **现状** | `shousi`（寿司，`cool`）与 `gali`（咖喱，`hot`）各是**一条**条目，而内部含明显不同的子类型：寿司其 `note` 原文即写「**生食款更偏寒**」；咖喱含日式/泰式/印式等辣度差别很大的做法 |
| **为什么值得考虑** | 在 B4 的逐条演算里，这两条的「基底 + 加工」**最难一次写清**：寿司的「生食」修正只对生食款成立；咖喱的「辛辣 + 深度」档位随品类变。若不拆，`note` 只能写成一句不精确的通述 —— 而 B4 的验收标准是「照 `note` 能推出同一个值」|
| **⚠️ 与既有裁定冲突的点** | `shousi` 的 `review_note` **已经裁定过一次**：「**去掉 raw 变体**：生食为常见吃法，属性维持凉，**不与生鱼片同档**」。**拆条会把「生食款」重新变回一个待决问题**（那一款该不该取寒），所以拆条前必须先把这条裁定的**适用范围**说清 —— 它约束的是「寿司整体」，还是「按常见吃法取值」 |
| **当前处置** | B4 **不拆**，只留痕 |
| **需要什么** | 三选一：**(α) 维持单条** —— `note` 写明「按键餐时的常见吃法取值」；**(β) 拆条** —— 寿司拆生食款/熟食款、咖喱按辣度拆（两者都可参照 D6「葱拆部位」的三选一模式）；**(γ) 只收窄匹配词** —— 把子类型词从 `aliases`/`keywords` 里清掉，让它们走表外的推测分支 |
| **验收标准** | 三选一被明确选定；若选 (β)，须同步处理 `shousi` 的既有 `review_note` 裁定，不能两条并存 |
| **相关文件** | `core/data/food_properties.json`、`docs/food-properties-b4-derivation.md`、`docs/food-properties-b4-derivation-plan.md` |

## D12　13 条旧茶饮的 `note` 仅 4–9 字（`note` 温度通道对它们是开着的）

| | |
|---|---|
| **裁定（2026-09-21，本条关闭）** | 选 **(β) 冻结现状**，**零数据改动**。实测三条（只读探针，不入库）：① 全表 `note` 非空且 <18 字**正好 13 条**，与 `SHORT_NOTE_LEGACY_WHITELIST` **集合完全相同**（双向都对得上）；② 这 13 条被 `note` 温度通道改过的 **0 条** —— 「菊花性凉」的「凉」不是温度**前缀**，通道不触发；③ 全表 `note` 通道命中 **0**。⇒ 安全不来自门槛（这是事实），但**当前也确无危害**。补长要付**两边**代价：13 条数据（走 `patch_food_table.py` 通道）＋它们进 Agent1 提示词的那句「说明：…」；且该守卫是**双向**断言（短 note 必须在白名单、白名单里不能有多余）⇒ 补长就必须**清空白名单**。⇒ **等 A1 审核那一轮顺手做** —— 审核人本来就要逐条看 `note`，那时补长不额外付一遍成本。 |
| **现状** | `juhua_cha`／`gouqi_cha`／`meigui_cha`／`lvcha`／`hongcha`／`damaicha`／`chenpi_cha`／`jiangcha`／`hongzao_cha`／`juemingzi_cha`／`heye_cha`／`luohanguo_cha`／`wumei_cha` 这 13 条的 `note` 只有 **4–9 字**（本批之前就如此），全部 ≤ `NOTE_MAX_LEN`(10) |
| **为什么值得考虑** | `note` 是**运行时字段**：`resolve_temperature_fields(name, note)` 在 `note` 去空格 ≤10 字时才会扫它。B4 给 53 条派生 `note` 定的纪律是「**≥18 字**」——越过门槛即通道恒不生效。这 13 条是唯一没被该纪律覆盖的形态：它们**此刻恰好**不含温度前缀（实测全表 0 命中），但安全来自运气而非门槛 |
| **当前处置** | B4 **不补长**（属 L1／L3 条目，超出本批范围），只在守卫 ①-c 里把 13 条**点名登记**：新增短 `note` 即变红、白名单腐烂也变红 |
| **需要什么** | 二选一：**(α) 补长** —— 把 13 条 `note` 写到 ≥18 字（白名单随之清空；注意同时会改它们进提示词的那句「说明：…」）；**(β) 冻结现状** —— 保留白名单 + 已有守卫 |
| **验收标准** | 白名单为空（全部 ≥18 字），或白名单与实测**完全一致**且守卫在位 |
| **相关文件** | `core/data/food_properties.json`、`core/tests/test_b4_derivation_notes.py`、`docs/food-properties-b4-derivation.md` |

## D13　核验单不渲染 B4 的 `note` 与冲突登记

| | |
|---|---|
| **现状** | `docs/food-properties-review-sheet.md`（508 行）只渲染 `sources.json` 里第一批 ② 层那 4 条的 `note`；B4 的 **53 条 `note` 与 `_meta.derivation_conflicts` 完全不出现在核验单里**（实测「派生：」「属性冲突」各出现 **0** 次，「口径分歧」出现 4 次） |
| **为什么值得考虑** | A1 的人工审核**以核验单为唯一依据**。派生依据与 4 条「待裁定」冲突若不在核验单里，审核人看不到 —— 它们目前的人类可读出口只有 `docs/food-properties-b4-derivation.md` §4 与提示词里的「说明：…」。方案 §5 甲案原写「审核人看得到」，实测**不成立** |
| **当前处置** | B4 未改核验单脚本（改它会动核验单结构与它自己的守卫），只把结论回写进 B4 文档 §10「事实更正 2」 |
| **需要什么** | 给核验单**新增一节**渲染 `_meta.derivation_conflicts`（`current`／`derived`／`reason`／`status`），并加一条「该节与 `_meta` 一致」的守卫 |
| **验收标准** | 核验单出现冲突节；`build_food_review_sheet.py --check` 通过；冲突节与 `_meta.derivation_conflicts` 的一致性有守卫 |
| **相关文件** | `core/scripts/build_food_review_sheet.py`、`docs/food-properties-b4-derivation.md` |

**解决于** 2026-09-19。

核验单新增两节：**§4.5 L2 派生依据**（B4 的 53 条，逐字渲染数据 `note`）与
**§4.6 派生冲突登记**（4 条，渲染 `_meta.derivation_conflicts` 的 `name`／`current`／`derived`／`reason`／`status`）；
补 3 条守卫（§4.5 逐字一致 ＋ §4.6 字段一致 ＋ 负控制），**变异检验 2/2 全红、还原后回绿**。
核验单 542 → **615** 行；`pytest` 496 → **499 passed**。

改动：`core/scripts/build_food_review_sheet.py`、`core/tests/test_food_review_sheet.py`、`docs/food-properties-review-sheet.md`。
⚠️ 顺带修正既有守卫 **§4.4 的 section 边界** —— 它原先只认二级标题 `##`，于是新增的三级节
§4.5／§4.6 会被算进 §4.4 的正文（§4.4 的 id 集合守卫因此假红）。现在边界同时认 `###` 与 `##`。

## D14　大麦茶的「制品口径」未定

> 2026-09-19 做 B5（无覆盖条目收口 19 条）时确认。它是登记表里**唯一**一条判据为
> 「制品口径未定」的 ③ 条目 —— 与其余 16 条不同：那些是**查不到**来源，这一条是
> **项目还没定用哪把尺子量它**。

| | |
|---|---|
| **现状** | `docs/food-properties-sources.json` 的 `damaicha` 现为 `layer: ③`、`status: 无源可引（制品口径未定）`、`mapping_review.accepted: false`；而 B4 的逐条演算把它**判归 L1**（语料有据）⇒ 同一套文档里有两种说法并存 |
| **为什么不能直接继承** | 两条阻碍**同时**成立：① 50 号文件 §5.2 明令「凡『茶』『糖』『汁』『粉』类后缀，先确认它是新制品还是同一物，**不可默认继承原料的四气**」；② 即便认继承，**原料本身就是多值的** —— 《本草纲目》行18934【气味】「咸，**温、微寒**，无毒」，同条两值不可择一。另注：B1 那 11 条茶饮的继承裁定**不适用**于它（那些的原料是药食同源饮片、有药典对照背书）|
| **需要什么** | 二选一：**(α) 认继承** —— 把「大麦」加进 `_meta.corpus_ancestors` 白名单，并**先定取「温」还是「微寒」**，本条改判 ① 或 ②（改判须同步重生成核验单）；**(β) 认独立制品** —— 须给出可引来源，否则维持 ③ |
| **验收标准** | 口径选定并落到 `docs/food-properties-sources.json`；该条的 `how`／`layer`／`status` 三者自洽；B4 文档与 B5 文档对该条不再并存两种说法 |
| **相关文件** | `docs/food-properties-sources.json`、`docs/food-properties-b4-derivation.md`、`docs/food-properties-b5-l3.md` |

**连带**：该条 `note` 仅 9 字，是 **D12** 点名登记的 13 条短 `note` 之一（`note` 温度通道对它开着）。

## D15　B5 新增的 2 条 ② 口径分歧（橘子／猪肉）等裁定

> 2026-09-19 做 B5 时判 ② 的 2 条。**值一律未动**，本项是它们等裁定的留痕处。

| | |
|---|---|
| **现状** | 两条都是「映射成立、值与来源不一致」：`juzi` 橘子（项目「凉」 vs 纲目「温」，**差两档且方向相反**）、`zhurou` 猪肉（项目「平」 vs 纲目「冷／微寒」，差一档）|
| **为何单列** | 二者本就属 **A1** 的 ② 层裁定范围，此处单列只为记账「B5 新增了哪 2 条」—— 免得 19 条收口里只有判 ③ 的 16 条有痕迹、这 2 条被误当已结案 |
| **依据强度** | 两条都**只有《本草纲目》一个来源**（50 号文件 §5.4 把「橘、柑、橙、柚」与「猪肉」都列入「仅经典有载、教材无」）⇒ 引用必须写明《本草纲目》，不得写成教材记载；`zhurou` 还须写明是行37623 **同一行内哪一句**（「冷」还是「微寒」），该行属 §6.2「纲目条目内的引录矛盾」|
| **需要什么** | 审核人给出逐条结论：以项目值还是来源值为准。若改值，走 `core/scripts/patch_food_table.py` 的正规通道，不手改 JSON |
| **验收标准** | 两条的 `nature_match`／`delta`／`status` 与裁定结果一致；核验单重生成后 `--check` 通过 |
| **相关文件** | `docs/food-properties-sources.json`、`core/data/food_properties.json`、`docs/food-properties-b5-l3.md` |

## D16　`suantou` 的 `id` 与 `name` 不一致（疑似既有笔误，登记不改）

> 2026-09-19 做 B5 逐条核对时撞见。**本项只登记，不改数据** —— 改它比看起来重。

| | |
|---|---|
| **现状** | `core/data/food_properties.json` 里 `"id": "suantou"` 的条目，其 `"name"` 是 **「米粉」** —— id 指向蒜、name 却是米粉 |
| **为什么是「登记不改」** | ① B5 的边界是「只登记来源、不动数据表」，`core/data/` 一字未动；② 改 id 或 name **不是笔误修正那么轻**：`_find_entry` 第 0 级是「输入 == `name`」直取（`exact_name_hit`），**改名会改判层**；id 又经 `keywords`／`aliases` 合并扫描被间接引用 ⇒ 动它前要先算清哪些词会改判 |
| **⚠️ 需要先确认哪一边错** | 是 **id 写错了**（应为米粉的 id）还是 **name 写错了**（应为「蒜」）—— 这决定改哪边、以及会不会与既有的「大蒜」条目撞车。另注：`core/scripts/patch_food_table.py` 的 `ENTRY_FIXES` 是**强制写入**，任何数据改动必须同步改它，否则被静默写回 |
| **需要什么** | 项目所有者判定 id／name 哪个为正、是否与既有条目重复；确认后走 `core/scripts/patch_food_table.py` 通道 |
| **验收标准** | 全表不再出现「`id` 与 `name` 指向不同物」的组合；`pytest` 全绿 |
| **相关文件** | `core/data/food_properties.json`、`core/scripts/patch_food_table.py`、`core/app/services/food_lookup.py`、`docs/food-properties-b5-l3.md` |

## D17　B5 新发现的 2 条别名同族混入（橘子含「橙子／柑」、鱼含「鲈鱼／草鱼」）

> 2026-09-19 做 B5 时扫出。与 **D4**（白酒混黄酒）、**D5**（糖混红糖）**同族**：
> 一个词挂在别的条目上，于是以 0.9 置信度呈现一个不属于它的四气。

| | |
|---|---|
| **现状** | 两条的 `aliases` 各挂了**与本体不同的物**：`juzi`（橘子）含「橙子」「柑」，而纲目「柑」（行24716／【气味】行24724「甘，**大寒**」）与「橙」（行24738／行24745「酸，**寒**」）是另两条条目、四气与「橘实」之**温**各不相同；`yuxia`（鱼）含「鲈鱼」「草鱼」，而「鱼」是无条目的现代通称 |
| **后果** | 用户说「橙子」「鲈鱼」会拿到该条的四气，且因属「表内命中」界面按 **0.9** 呈现并标「已收录」 |
| **当前处置** | B5 **只登记来源、不动数据**；两条的 `open_question` 已写明「须另案裁定」—— 本条即那个「另案」|
| **需要什么** | 二选一：**(α) 从 `aliases` + `keywords` 两处移出**（`_find_entry` 第 2 级把两处**合并扫描**，只删一处无效），这些词改走表外的推测分支（0.3，界面标「食性未经验证」）；**(β) 各另立条目**（橙／柑／鲈鱼／草鱼），但**各须先有可引来源**，不能拿橘子的依据顶 |
| **验收标准** | 二选一被明确选定并落到数据；「一个词挂在不属于它的条目上」这一状态不再**无声**地存在 |
| **相关文件** | `core/data/food_properties.json`、`docs/food-properties-sources.json`、`core/app/services/food_lookup.py`、`docs/food-properties-b5-l3.md` |

## D18　冲突处理框架（冲击度轴 + 体质×极端轴）的落地实施

> 2026-09-20 由所有者逐条裁定口径后登记。属**新建**一层能力，不是修补既有逻辑——
> 实测 `nature_math.combine()` 在**生产链路零调用点**（只有判定层测试调它），
> 现状是「方向相反」的一餐在离线路径**塌陷成 unknown**（不是判成「平」），
> 所以 ① 的实质是**新增一个显式状态**，不是修一个加法 bug。
>
> **2026-09-20 追加裁定（定位级）**：本项目只做**事后处理**（「吃了怎么办」），不做**事前提醒**（拦／挡／报警）。⇒「体质 × 极端」20 项里的 **#7／#16（特禀 × 发物）** 是事前逻辑（忌／避开），**移出**；仲裁顺序简化为 **冲击度 ≥2（第一优先级：护脾胃）→ 体质 × 极端方向（第二优先级）**，**不设硬禁忌档**。用户自述「吃某物过敏」走既有高风险分支（`HIGH_RISK_KEYWORDS` 已含「过敏」；实测「我对虾过敏」命中），不算发物拦截。

| | |
|---|---|
| **现状** | 口径**已全部裁定**；**阶段 0／1／2 已全部落地**，阶段 3（近 24 小时累积）**独立立项**（`AnalyzeRequest` 无历史、`session_id` 只作日志串联、全仓无持久化 ⇒ 需会话状态存储，不是加个字段）。阶段 0：`bde299e`；阶段 1：`ef23bdc` 装配进 `ParsedMeal.signals`、`81c6cdd` 改提示词第 3 条、`c453f07` 两个壳渲染 + 终端离线链路接线；**阶段 2（4 次提交）**：`9690072` 新增 `core/data/meal_plan_rules.json` 与纯函数 `derive_meal_plan`（`core/tests/test_meal_plan.py`，零行为变化）、`50c5b41` 离线链路接上优先级链 L0–L4 并移除 `RULES.sweet_heavy`（**D25**）、`84410e2` 提示词第 1／3／7 条改「两段推荐 + 只给一条」并把 `plan` 接进 LLM 与离线两条链路、`ab7e3d9` 两个壳渲染 `plan.note`。前置的离线路径串味问题已修（见 **E15**，commit `1902fb6`） |
| **口径（已定，10 条）** | ① 冲突时 `ParsedMeal.overall_nature` 仍取 `unknown`，**另加并列新字段**——`Nature` 枚举**不动**（它被 `_NATURE_TO_NUM`／`NATURE_LABELS`／数据取值域复用，加值等于改四气轴本身）；② 冲击度落 **`ParsedMeal`**（不落 `AnalyzeResponse` 顶层：`build_user_prompt` 把 `parsed.model_dump_json()` 整个塞进提示词，落这里 Agent2 零接线）；③ 判据分三档 full／partial／none，并派生 `evidence_floor`（供渲染位诚实标注）；④ 规则表落**新数据文件**，不动 `food_properties.json`；⑤ 高糖／碳酸／酒精／黏腻标 `evidence: "none"`（食性表 `FIELD_ORDER` 无糖量/碳水/加工工艺字段，真没依据）；⑥ 护脾胃具体药材**暂不定**，落成空集合 +「空 ⇒ 不开启」，照 `ready_constitutions()` 先例，**不静默退化**；⑦ 两段推荐须在**离线路径显式接线**（否则重演 E4：有 Key／没 Key 安全边界不同）；⑧ 优先级由**代码派生**（纯函数），不由模型自报；⑨ 「平和 × 极端」以**不建议对冲**为准，须**改 Agent2 提示词第 3 条**（现写「寒热要平衡：吃了燥热的配清凉的」，与⑨直接矛盾）；⑩ 阶段 3 的「近 24 小时累积」**独立立项**（`AnalyzeRequest` 无历史、`session_id` 只作日志串联、全仓无持久化 ⇒ 需会话状态存储，不是加个字段） |
| **落地顺序** | **阶段 0**（纯函数 + 新数据文件 + 测试，**不改既有行为**）→ **阶段 1**（接 `ParsedMeal` + 两个壳 + 提示词，只提示不改方向）→ **阶段 2**（两段推荐，含离线接线 + 护脾胃开关）→ **阶段 3**（24h 累积，独立立项）。**前三阶段已完成（2026-09-20）** |
| **为何分阶段** | 阶段 0 可以只加函数与数据、零行为变化，因此**能独立验证**；一旦把「接线 + 提示词 + 两个壳」混进去，就分不清某条输出来自新公式还是模型措辞 |
| **需要什么** | 所有者按阶段放行；阶段 1 起涉及两个壳的**显式渲染**（`ui/web/index.html` 手写字段、`ui/terminal/chat.py` 显式 print——不加就是「改了数据、用户看不到」，即 **E7** 的形状） |
| **验收标准** | 每阶段 `pytest -q` 全绿且**阶段 0 不改变任何既有输出**（用「改前/改后全量探针」证明） |
| **相关文件** | `core/app/domain/nature_math.py`、`core/app/services/food_lookup.py`、`core/app/domain/safety.py`、`core/app/services/orchestrator.py`、`core/app/agents/prompts/agent2_system.md`；**阶段 0／1 已创建**：`core/app/domain/diet_signals.py`、`core/data/diet_signals.json`；**阶段 2 已创建**：`core/data/meal_plan_rules.json`（方向与文案的数据真源）、`core/tests/test_meal_plan.py`、`core/tests/test_meal_plan_wiring.py`、`core/tests/test_meal_plan_prompt.py`、`core/tests/test_shell_plan_render.py` |

## D19　整餐偏性是否收归确定性（接线 `nature_math.combine()`）

> 2026-09-20 评估「冲突处理框架」时实测发现。所有者裁定**独立立项，本轮不做**。

| | |
|---|---|
| **现状** | `combine()` 在**生产链路零调用点**，只有 `core/tests/test_nature_math.py` 调它 ⇒ **整餐偏性现由模型自拍，或由离线路径的关键词打分**（`analyze_offline` 的 `_match_nature_from_text`），没有确定性合成。反向极端在离线路径塌陷成 `unknown` 后，`_pick_rule` 的 `overall_nature in match_natures` 只剩「夜宵」一条能命中 |
| **为什么值得单列** | 这是「同一餐、有没有 Key 拿到不同偏性」的根源之一，且**不属于**冲突框架（冲突框架只加「错杂」这个显式状态，不负责把加法接上） |
| **为什么本轮不做** | 接线会**改变既有输出的偏性取值**，影响面覆盖全部场景；与本轮「只加显式状态、不改既有方向」的取向冲突 |
| **需要什么** | 所有者裁定：接线 `combine()`／维持现状（模型判）／第三条路（只对离线路径接）。若接线，须先定「主导方向 + 反向极端」与 `unknown` 的优先级关系（与 **D18**① 相邻） |
| **验收标准** | 明确选定其一；若接线，须有「改前/改后全量探针」证明影响面 |
| **相关文件** | `core/app/domain/nature_math.py`、`core/app/api/analyze_offline.py`、`core/app/services/matcher.py`、`docs/three-layer-architecture.md`（§已知局限 3 自认第二层组合推理未接入主流程） |

## D20　发物名单落地（有本地依据的上位类别可落；表外项记 not_in_catalog）

> 2026-09-20 由所有者逐条裁定后登记。评估稿见 `core/var/` 下的 fawu-assessment.md。
> **缺的是逐项依据与系统词表，不是实现**。
> **2026-09-20 补充裁定：本条关闭 ——「发物」是事前提醒逻辑（「你别吃虾」），本项目只做事后处理
> （「你吃了虾，那喝点什么」）。不落数据、不加字段、不加机制，也不做单独的「发物提醒」。**
> 以下「已定的落法／为什么不能现在落／需要什么」保留为改判前的分析留痕。

| | |
|---|---|
| **裁定（2026-09-20，本条关闭）** | **发物不单列，当普通食物处理。** 定位理由：发物属**事前提醒**逻辑，而本项目是**事后处理**工具——用户说「我吃了虾」就走正常的「吃了什么 → 推荐茶方」链路，不需要单独的发物提醒。原评估的三条实测事实（食物侧无体质通路、名单覆盖约 55%、本地语料只有上位类别有据）反过来也支持不落：**没有一份可审定名单**。**边界**：用户主动说「我吃虾过敏」属「用户自述过敏」，走既有高风险分支（`HIGH_RISK_KEYWORDS` 含「过敏」），**不是发物拦截** |
| **现状** | 名单已裁定**只落方向、不落配方**，且**只落食物侧**（饮片侧不落）。三条实测事实：① **食物侧没有体质通路、也没有 `cautions`**（`foods` 业务字段只有 id/name/aliases/category/nature/flavors/keywords/variant_nature/note，`check_constitution_fit` 只读饮片）⇒ 是「通路没有」而非「字段没填」；② 名单对食物表**覆盖约 55%**——硬拦截 11 项里 **5 项食物表压根没有**（荞麦／蚕豆／鹅肉／鲤鱼／浓茶），另有 ⑦光敏类 9 项里 5 项没有（茴香／苋菜／荠菜／香菜／菠萝）；③ 本地语料「发物」共 **6 处**，**只有上位类别（腥膻／海腥／鱼虾蟹／辛辣刺激）有据**，且语境全是**病证或服药忌口**，而所引《中国公民中医养生保健素养》**不在项目实施语料里**、所引那句也只是概括性建议、**不含名单** |
| **已定的落法** | ① 只落**有本地依据的上位类别**，其余一律标「外部专业意见·未经审核（待补依据）」；② 表外项**显式记 not_in_catalog**（不静默漏判——「落一半」比「不落」更糟）；③ 与既有轴重叠的类别（助火／助湿 ⊂ 冲击度，冷积 ⊂ 温度前缀层，滞气含的莲子／芡实本就是白名单饮片）**声明归属既有轴**，不重复计分；④ 名字重复项合并（组合清单里「特禀 × 发物」出现两次、阴虚出现「温燥／辛燥」两次） |
| **为什么不能现在落** | 逐项依据不齐 + 系统词表缺近半 + 依赖尚未开工的识别器（**D18** 阶段 0）⇒ 现在写进数据就是「用通行表述伪装权威依据」 |
| **需要什么** | ① 上位类别的**可复现依据行号**（本项目语料内）；② 其余类别的外部依据来源（所有者提供或标注待补）；③ 阶段 0 的识别器就位后接入 |
| **验收标准** | 每条落进数据的信号都能指出依据、或显式标「未经审核」；表外项在数据里有 not_in_catalog 留痕；`pytest -q` 全绿 |
| **相关文件** | `core/app/services/food_lookup.py`、`core/app/domain/safety.py`；拟新增（未创建）：core/app/domain/diet_signals.py、core/data/diet_signals.json、core/data/constitution_extremes.json。**饮片侧 `core/data/herbs.json` 不动**（49 号文件 §5.3 已裁定菊花／玫瑰花／槐花／茉莉花不得标为特禀不宜；§5 判据只认「经口给药的过敏记载」） |

## D21　食物侧缺体质消费通路（`check_constitution_fit` 只读饮片）

> 2026-09-20 做发物名单评估时实测发现。**结构性缺口**，非本批引入。

| | |
|---|---|
| **现状** | 全仓唯一做「食物 × 体质」判定意图的函数 `check_constitution_fit` 的实现是：遍历传入**饮片** → `herb_by_name()` 查条目 → 取 `unsuitable_for` 与体质求交。**食物侧完全不参与**；`foods` 与 `tea_drinks.items` 也**没有** `unsuitable_for` / `cautions` 字段。⇒ 结论是「食物 × 体质」这一层**不存在**（不是「字段没填」） |
| **为什么值得单列** | **D18 压在它上面**：冲击度、整餐冲突都是「食物侧」的判定，需要一条确定的落点与消费路径。不先定它，两条新轴会各自发明一套食物侧通路（原并列的 D20 发物名单已裁定关闭，不再依赖本条） |
| **为什么本次不动** | 新建一层是**范围决定**，不是修 bug：要定「食物侧标记是新建数据文件还是扩既有数据表」「消费点在编排层哪一步（高风险分支之后？体质就绪闸门之前？）」 |
| **需要什么** | 所有者定范围，三选一：**(α) 新建食物侧标记文件 + 编排层新消费点**（不动既有食性数据）；**(β) 扩 `food_properties.json` 字段**（需回写 129+18 条、同步字段顺序与条目修正表，属既有数据改动）；**(γ) 维持现状**，三条新轴各自在编排层就地判定、不落食物数据 |
| **验收标准** | 三选一被明确选定；此后三条新轴的落点不再**无声**分歧 |
| **相关文件** | `core/app/domain/safety.py`、`core/app/services/orchestrator.py`、`core/data/food_properties.json`、`core/scripts/patch_food_table.py`（字段顺序与条目修正表） |

## D22　硬拦截的落地形状（口径已裁定为「提示 + 方向置顶」）

> 2026-09-20 评估发物名单时裁定。**口径已定、实现未做**，故登记为「待实施」而非「待确认」。
> **2026-09-20 补充裁定：本条关闭 —— 不拦截、不屏蔽、不报警，也不做单独的「发物提醒」。**

| | |
|---|---|
| **裁定（2026-09-20，本条关闭）** | 所有者改判：**本项目只做事后处理，不做任何事前拦截**。改判前的 (c)「提示 + 方向置顶」**作废**——置顶本身就是一种拦截强度，与「发物不单列」不一致。**不做** `HIGH_RISK_KEYWORDS` 增项（不加「发物」：它不是「需就医」的情况）、**不新增**字段、**不改**壳端渲染、**不加**软提醒。原「硬拦截」的性质判别保留为分析留痕：本项目唯一的确定性闸门是高风险分支（纯关键词、不调模型、不拒绝服务），说「我过敏」今天就走那条路 |
| **现状** | 本项目**没有「硬拦截」的先例含义**。唯一的确定性闸门是高风险分支：**纯关键词扫描、不调模型、不花钱**，返回空推荐 + `user_message` 提示，**不拒绝服务**。而 `HIGH_RISK_KEYWORDS` 里**已含「过敏」**——说「我过敏」今天就已经走那条路，说「发物」不会 |
| **已裁定的口径** | 选 **(c) 提示 + 方向置顶**：不拒答、不从饮片候选集里剔除，只把方向与提醒**置顶且不可省略**，「硬」体现为**压过第二优先级（体质 + 整餐偏性）的推荐方向**。理由：① 不需要新依据（现状无依据可剔）；② 不碰饮片候选集（特禀质 `suitable_constitutions` 只有 2 味，多屏蔽一味有压空风险）；③ 与「落方向不落配方」同构 |
| **为什么登记为待实施** | 口径定了但机制未建：落点、渲染位、与 **D18** 仲裁顺序（C 硬禁忌 > 冲击度 ≥2 > C 方向）的接线都还没做 |
| **需要什么** | ① 落点定在哪一层（须排在高风险分支之后、凭据校验之前，且**不依赖模型**，否则重演 E4）；② 输出字段与**两个壳的显式渲染**；③ 与 **D18／D20** 一并实施 |
| **验收标准** | 两个壳都能看到该提示；无 Key 路径同样生效（用无 Key 探针证明）；`pytest -q` 全绿 |
| **相关文件** | `core/app/services/orchestrator.py`、`core/app/domain/safety.py`、`core/app/domain/models.py`、`ui/web/index.html`、`ui/terminal/chat.py` |

## D23　湿气生成度落地（先落高糖／高油／生冷／乳制品 4 维）

> 2026-09-20 由所有者逐条裁定后登记。评估稿见 `core/var/` 下的 dampness-assessment.md。

| | |
|---|---|
| **现状** | 「湿气生成度」回答的是「今天要不要额外加化湿的药材、加多少力度」，与「体质 × 饮食方向」是两条独立的轴（举例：湿热质 + 一小块蛋糕 vs 湿热质 + 三块奶油蛋糕加冰奶茶，体质方向相同、力度完全不同）。**阶段 0／1／2 后这条轴已落地**：高糖／高油／生冷／乳制品 4 维由 `core/data/diet_signals.json` 的 `signals` 派生（阶段 0），湿气度 ≥2 时由 `core/data/meal_plan_rules.json` 的「化湿」方向接管离线搭配（阶段 2，`50c5b41`），可用饮片 = **方向子集 ∩ `filter_by_constitution` 候选集**（空集即关闭并如实说明）。⚠️ **份量仍未进决策**（见 **D26**）⇒ 现状是「**有方向、无力度**」：「一小块蛋糕」与「三块奶油蛋糕加冰奶茶」目前拿到的是**同一组化湿搭配**，力度尚未区分 |
| **已定的落法** | ① **先落 4 个能从既有字段派生的维度**：高糖、高油、生冷、乳制品（`category` 已填 129/147，`乳饮`／`甜点`／`冷饮`／`甜汤`／`酒类`／`饮料` 正好被单独分出）；② 第 5 维「黏腻」**表内零条目** ⇒ 不落，见 **D27**；③ **不改食物表**（不加字段、不动既有条目）；④ 规则参数（触发与权重）落**数据文件**，不写代码常量 |
| **为什么当时分阶段** | 阶段 0 本体未开工，且依赖 **D24** 的信号结构（先把「信号定义一次」落定，否则两维各建一套识别器）。**该前提已于阶段 0 落地**——`core/data/diet_signals.json` 的 `signals` 是原子信号、只定义一次，`dimensions` 每维各带 `weights`／`aggregation`／`levels` ⇒ 本条**已随阶段 0／1／2 实施**。遗留两条：**D26**（份量不进决策 ⇒ 只有方向没有力度）、**D27**（黏腻维表内零食物，该维未落） |
| **需要什么** | ① 阶段 0 的 `diet_signals` 模块就位；② 阶段 2 的离线接线（在线与离线**用同一个纯函数**，否则重演有 Key／没 Key 边界不同）；③ 加药路径**必须过** `filter_by_constitution`，见下 |
| **验收标准** | 同一体质下，「少量」与「大量」两组口述能得到**不同的湿气度**；湿气度 ≥2 才有「化湿」方向的建议；加药候选集必须来自 `filter_by_constitution`（有测试钉住）；`pytest -q` 全绿 |
| **相关文件** | `core/app/services/matcher.py`、`core/app/api/analyze_offline.py`、`core/app/services/orchestrator.py`、`core/app/agents/prompts/agent2_system.md`；**已创建**：`core/app/domain/diet_signals.py`、`core/data/diet_signals.json`、`core/data/meal_plan_rules.json`。饮片侧 `core/data/herbs.json` **未动**（化湿方向的饮片来自既有白名单 ∩ 候选集，没有新立条目） |

## D24　信号结构升级：原子信号只定义一次，每个维度各带权重与聚合

> 2026-09-20 裁定。**这是两条新轴的结构前提**，不是可选项。

| | |
|---|---|
| **现状** | 项目里已经有一处「信号 + 权重」的雏形：`matcher.py` 的 `RULES` 每条规则自带 `keywords` 与 `match_natures`。但它们是**场景规则私有**的，也没有跨条目（per-meal）的概念 |
| **为什么值得单列** | 同一个现象会同时喂两条轴：**「冰镇」既算脾胃冲击度、也算湿气生成度**（刻意重复，是设计不是 bug）。若两条轴各建一套识别器，同一现象会在两处各定义一次、然后漂移。⇒ 「刻意重复」应落在**权重层**，**不在定义层** |
| **已定的落法** | ① 结构分两层：`signals`（原子信号，**只定义一次**）+ `dimensions`（每个维度各带 `weights`／`aggregation`／`levels`）；② 新增一个概念：**信号元数（arity）** ——「寒热急变（热食 + 冰饮同餐）」是 **per-meal 跨条目**信号，不是单条目属性（否则「火锅配冰饮」会被拆成两条各自无信号）；③ 识别器**只长在 `match_foods` 的表内命中上**，不裸扫原文 |
| **为什么本轮不做** | 阶段 0 本体未开工 |
| **需要什么** | 阶段 0 落地；同时把 `RULES.*.keywords` 的既有词表**收编进 signals**（否则新老两套词表并存） |
| **验收标准** | 派生不变式：`signals` 里每个 id 至少要有一个能触发它的样本（加信号忘加样本 ⇒ 测试变红）；每个 signal 至少被一个维度用到；`pytest -q` 全绿 |
| **相关文件** | `core/app/services/matcher.py`、`core/data/food_properties.json`（`category`／`flavors` 是派生源）；拟新增（未创建）：core/data/diet_signals.json、core/app/domain/diet_signals.py |

## D25　湿气轴取代 `RULES.sweet_heavy`（消除两套化湿逻辑）

> 2026-09-20 裁定：**取代**（不是并存、不是暂不动）。

| | |
|---|---|
| **现状** | ✅ **已实施（2026-09-20，阶段 2 `50c5b41`）**：`RULES.sweet_heavy` **已从 `RULES` 移除**，其位置由湿气度 ≥2 的「化湿」方向承担（方向与文案在 `core/data/meal_plan_rules.json`）；`agent2_system.md` 那句同名表述**已同步**（第 1 条改两段推荐，方向由代码派生的 `parsed.plan` 说出，模型不再自报）。⚠️ **新旧形状不同，不是「换了条规则」而是换了机制**：原 `sweet_heavy` 是**一条写死的搭配（茯苓 6 + 陈皮 4）且完全不过体质**；现在的化湿方向是「**方向子集 ∩ `filter_by_constitution` 候选集**」——**过体质**，且**空集即关闭并如实说明**（实测阴虚质下该交集为空 ⇒ 关闭，不再送搭配） |
| **为什么值得单列** | 这是一处**行为变更**（会改变既有离线输出），与「阶段 0 只加不改」的取向不同，必须单独可见、单独验证 |
| **已定的落法** | 湿气轴接管「甜腻」判据；`RULES.sweet_heavy` 从 `RULES` 中移除，其位置由湿气度 ≥2 的「化湿」方向分支承担。**同时**要处理 `agent2_system.md` 那句同名表述，否则提示词与代码说的不是一回事 |
| **为什么当时单列** | 这是一处**行为变更**（会改变既有离线输出），与「阶段 0 只加不改」的取向不同，必须单独可见、单独验证 ⇒ 落在阶段 2，并用「改前／改后全量探针」量出影响面（这正是 E4 的教训：两条路径边界不同）。**实测**：90 条样本里 39 条变化；第一版把化湿写成「无条件压制场景规则」时是 69 条变化，加 `overrides_scene` 字段（护脾胃＝能压／化湿＝不能压）后降到 39 |
| **需要什么** | ① 阶段 2 的离线分支就位；② 「改前／改后全量探针」，证明移除 `sweet_heavy` 的影响面（这正是 E4 的教训：两条路径边界不同） |
| **验收标准** | ✅ 已达成：`RULES` 里不再有 `sweet_heavy`；离线与在线两条路径都走同一个纯函数 `derive_meal_plan` ⇒ 对「甜腻」给出**同源**结论（比原定的「同向」更强：不是两边判成一样，是**同一个判定**）；改前／改后探针留痕；`pytest -q` 全绿 |
| **相关文件** | `core/app/services/matcher.py`、`core/app/agents/prompts/agent2_system.md`、`core/data/meal_plan_rules.json`、`core/tests/test_meal_plan_wiring.py`；**已删**：`matcher.py` 的 `RULES.sweet_heavy` 一条（删除处的注释留痕说明它去了哪里，按注释找、别按行号找） |

## D26　份量解析（`AnalyzeRequest` 无份量字段，「大量」无来源）

> 2026-09-20 登记。**两条轴都要用份量**（冲击度与湿气度各有一档「份量修正」），但现状解析不出来。

| | |
|---|---|
| **现状** | `AnalyzeRequest` **没有份量字段**（只有 `text`，上限 500 字）。用户说「三块奶油蛋糕加冰奶茶」时，「三块」归属**哪一样食物**是局部依存问题（与 E15 同族）；而「多少算大量」**没有任何来源**。实测：`'一小块蛋糕'` 与 `'三块奶油蛋糕加冰奶茶'` **命中同一条规则、返回同一组搭配** |
| **为什么本轮不做** | 归属问题需要「数量词 ↔ 食物」的局部依存解析，是独立课题；且「大量 ≥ 几」无依据 ⇒ 现在做就是编数字 |
| **已定的落法（本轮口径）** | 阶段 0 **只做布尔粗判 + 标 `evidence: none`**：识别到「三块／三杯／一大份」等量词就记命中，但**不定阈值、不计权重** |
| **需要什么** | ① 所有者定范围：是「新解析器」还是「把份量交给模型判」；② 「多少算大量」的依据来源（或显式接受「无据、只做粗判」） |
| **验收标准** | 所选方案明确；若维持粗判，则粗判不得影响既有输出（探针证明）；`pytest -q` 全绿 |
| **相关文件** | `core/app/domain/models.py`、`core/app/api/analyze_offline.py`、`core/app/agents/agent1_diet.py`、`core/app/agents/prompts/agent1_system.md` |

## D27　黏腻维度缺食物（食物表 147 条内无任何糯米制品）

> 2026-09-20 登记。**「扩展食物库」属另一条线**（要走字段顺序、条目修正表、回写 147 条与 A1 审核），与本轮「做冲突框架」不是一回事。

| | |
|---|---|
| **现状** | 湿气轴第 5 维「黏腻」的判据是「糯米 / 糯米粉制品」。实测：糯米、年糕、汤圆、麻薯、糍粑、粽子、八宝饭、驴打滚、艾窝窝、青团、米糕 —— **11 词在食物表里一个都没有**（147 条全名单内无任何糯米制品）。同类：`奶酪／芝士／黄油／奶油` 也没有独立条目（只是「牛肉汉堡／蛋糕」的子串），表内真乳制品只有 4 条 |
| **为什么值得单列** | 它让「湿气 5 维」实际只能落 4 维。不记下来，后来人会以为「漏了」而去找 bug |
| **为什么本轮不做** | 所有者裁定：**先不加食物，先登记不落**。扩食物表是独立的一条线（`FIELD_ORDER` + `ENTRY_FIXES` + 回写 147 条 + 过 A1） |
| **需要什么** | 所有者定范围：① 随下一次食物库扩展一起补糯米类；② 保持缺位并显式标 `not_in_catalog`；③ 不计入该维 |
| **验收标准** | 三选一被明确选定；若选 ②，数据里有显式的缺位留痕（不静默漏判） |
| **相关文件** | `core/data/food_properties.json`、`core/scripts/patch_food_table.py` |

## D28　终端离线路径 `meal_time` 写死 `UNKNOWN`

> 2026-09-20 在 S0（终端离线路径改用 `resolve_in_context`）里发现。**属设计问题不是 bug**，故单独登记、不夹在那次修复里。

| | |
|---|---|
| **现状** | `ui/terminal/chat.py` 的 `_offline_analyze` 写死 `meal_time=MealTime.UNKNOWN`；而 API 离线路径 `core/app/api/analyze_offline.py` 用 `_guess_meal_time` 从口述里猜（定义在 `analyze_offline.py:51`）。⇒ **同一句口述在两条链路的 `meal_time` 不同** |
| **为什么值得单列** | `matcher` 的场景规则（含 `RULES.late_night`）按 `meal_time` 命中 ⇒ 终端路径下夜宵场景永远命不中 |
| **为什么本轮不做** | `_guess_meal_time` 定义在 `core/app/api/` 下，终端要用得复制一份、或**从 api 层反向 import**（`ui/` 层只 import `app.*` 的领域层与服务层）⇒ 属分层设计问题 |
| **需要什么** | 所有者定：① 把 `_guess_meal_time` 下沉到 `core/app/domain/` 或 `core/app/services/`，两条链路共用；② 终端维持 `UNKNOWN`，并在界面上显式标注「未判定餐次」 |
| **验收标准** | 二选一被选定；若选①，两条链路对同一批口述的 `meal_time` 一致（探针证明） |
| **相关文件** | `ui/terminal/chat.py`、`core/app/api/analyze_offline.py`、`core/app/services/matcher.py` |

## D29　`鸡蛋` 的 `keywords` 含裸字「蛋」⇒「蛋糕」同时命中两条

> 2026-09-20 在阶段 1 勘察「输入质量」时实测发现。与 D4／D5／D17 同为「以 0.9 呈现不属于该条的四气」，但**成因不同**：那三条是别名／关键词混进另一物，这一条是**单字关键词在子串层面命中**。

| | |
|---|---|
| **现状** | `鸡蛋` 条目的 `keywords` 是 `['鸡蛋', '水煮蛋', '煎蛋', '荷包蛋', '蛋']`，末项是裸字。实测 `match_foods('蛋糕')` → `['蛋糕', '鸡蛋']`；`match_foods('下午吃了蛋糕喝了冰奶茶')` → `['冰奶茶', '奶茶', '蛋糕', '鸡蛋']`。同类：`卤肉` 的 `keywords` 含「卤蛋」 |
| **影响** | 展示里凭空多一样食物；整餐置信度（各食物置信度均值）被拉偏。**不太会翻转阶段 1 的 `conflict` 布尔**（「鸡蛋」是平性，进不了 ±2）⇒ 定级为「污染展示」而非「改变结论」 |
| **为什么本轮不做** | 属数据治理，要走 `core/scripts/patch_food_table.py` 通道并过 A1 审核；S0 的性质是代码修复 |
| **需要什么** | 所有者定口径：① 删掉裸字「蛋」；② 保留但立一条纪律——**关键词不得短于 2 字**（至少不得是常用复合词的尾字），并据此回头扫全表 |
| **验收标准** | 二选一被选定；改后 `match_foods('蛋糕')` 只返回「蛋糕」，且 `match_foods('水煮蛋')` 仍能命中 |
| **相关文件** | `core/data/food_properties.json`、`core/scripts/patch_food_table.py` |

## D30　「冰奶茶」被同时识别成「冰奶茶」与「奶茶」两条

> 2026-09-20 同上。它是 E15「温度词按位置归属」修好之后**露出来的下一层**：带温度前缀的条目与其基条目同时命中。

| | |
|---|---|
| **现状** | 实测 `match_foods('冰奶茶')` → `['冰奶茶', '奶茶']`。同一样东西在 `ParsedMeal.foods` 里记两次；带温度前缀的条目（`冰奶茶`／`冰可乐`／`冰啤酒`）都有这个现象 |
| **影响** | ① 展示重复；② 整餐置信度均值被重复项拉偏；③ **阶段 1 接上后，同一物的四性在冲突判定里计两次**（极端档会被放大，不是变小） |
| **为什么本轮不做** | 去重口径本身要定，且**不能简单合并**：「冰奶茶」与「奶茶」四气不同（冰的更寒），粗暴合并会把 E15 刚修出来的温度前缀成果抹掉 |
| **需要什么** | 所有者定口径：① 命中区间重叠即合并、**保留更具体的那条**（带前缀的）；② 按 `_entry_key` 相同即合并；③ 不去重，只在渲染层折叠 |
| **验收标准** | 三选一被选定并有测试；测试须含这条负控制——**合并不得把温度前缀抹掉**（「冰奶茶」合并后仍须寒于「奶茶」） |
| **相关文件** | `core/app/services/food_lookup.py`、`core/app/api/analyze_offline.py`、`ui/terminal/chat.py` |

## D31　`RULES.late_night` 靠**空关键词**兜底，把非夜宵的一餐说成「夜里吃得多」

> 2026-09-20 阶段 2 做「改前／改后全量探针」时实测发现。**本阶段只登记、不改**。

| | |
|---|---|
| **裁定与落地（2026-09-21）** | 选 **(β) 只改叙述，`(α)` 不做**（(α) 加时段判据依赖 **D28**：终端 `meal_time` 写死 `UNKNOWN`，做了夜宵会在终端链路永远命不中）。改动：该规则的 `label` 「夜宵偏和胃安神」→「未判出偏向·和胃安神」、`reason` 「夜里吃得多，…」→「这一餐没判出明显的寒热偏向，先用偏于理气和胃的温性搭配，量宜少。」**判据与搭配一点没动**。改前／改后探针（5 条样本）：仅那 2 条误命中样本的文案变化，`hits` 的 id、`title`、**搭配**全部不变。守卫见 `core/tests/test_late_night_wording.py`（6 项，含一条自检式变异检验：把文案改回含「夜里」⇒ 判据必须变红）。⚠️ 名字里的 `late_night` 是历史标识，它实际是「没判出场景」的**兜底**，不是夜宵规则 —— 这条已写进 `matcher.py` 的注释 | 
| **现状** | `RULES.late_night`（`id` 仍叫 late_night，`label` 已改为「未判出偏向·和胃安神」，陈皮 4 + 茯苓 6）的 `keywords` 是**空元组**，只靠 `match_natures={UNKNOWN}` 拿 1 分 ⇒ **任何「整餐偏性 unknown 且没命中任何关键词」的口述都会落到它**。实测（离线路径）：「中午吃了米饭和炒青菜」（`meal_time` 猜出 `lunch`）、「早餐吃了面包和牛奶」（猜出 `breakfast`）**都拿到 `late_night`**，理由写着「**夜里吃得多**，偏于理气和胃的温性搭配更稳妥，量宜少」——用户说的是午饭和早饭。⚠️ `_pick_rule` **从不读 `meal_time`**：离线路径已经能猜出时段，证据在手边却没用上 |
| **影响** | ① 给用户一句**凭空的时间叙述**，属「场景造假」而非搭配出错（陈皮 + 茯苓这组搭配本身不危险）；② 它是**兜底**、排在体质默认之前 ⇒ 一条「什么都没认出来」的口述会先拿到它，而不是该体质自己的默认搭配 |
| **阶段 2 做了什么（以及没做什么）** | **只绕开、不修**：L1（寒热错杂）与 L4（湿气触发但化湿方向不可用）现在都**退体质默认**，不再让 `late_night` 顶上——`_scene_rule_ids` 要求规则**必须有关键词命中**，`late_night` 关键词为空 ⇒ 被排除。实测「下午茶一块蛋糕和一杯奶茶」改前拿 `late_night`、改后走化湿方向。**老路（`signals` 缺失或 `plan` 为 None）原样保留** ⇒ 「米饭炒青菜」今天仍会中这条 |
| **为什么本阶段不修** | 修它要动 `_pick_rule` 的判据（改评分或把兜底拆出来），是**既有行为变更**；阶段 2 的影响面必须干净，否则「改前／改后」探针分不清哪条变化来自方向接管、哪条来自兜底改动（**E4** 的教训：两条路径边界不同就会不一致） |
| **需要什么** | 三选一：**(α) 给 `late_night` 加时段判据**（`meal_time == LATE_NIGHT` 才算），把「无场景结论」的兜底从 `RULES` 里拆出来另立分支；**(β) 保留兜底、改叙述**（标题与理由去掉「夜里」，只说「这一餐没认出明显偏向」）；**(γ) 取消 `UNKNOWN` 那 1 分**——`unknown` 是「判不出」，不该给任何规则加分。⚠️ **(α) 依赖 D28**：终端离线路径把 `meal_time` 写死 `UNKNOWN`，在它修好之前 (α) 会让真正的夜宵在终端链路永远命不中 |
| **验收标准** | 三选一被选定并有测试；测试须含**负控制**——「中午吃了米饭和炒青菜」不得再拿到任何含「夜里／夜宵」的叙述，**同时**「宵夜吃了烧烤」仍要拿到消食或和胃的结论（不能为了去掉夜宵，把真正的夜宵场景也一起弄丢） |
| **相关文件** | `core/app/services/matcher.py`、`core/app/api/analyze_offline.py`、`ui/terminal/chat.py`（**D28**） |

---

# E 类 · 技术债（开发可自行处理）

## E1　3 条食性表缺 `review_status`

> **状态：已解决（2026-09-17）** —— 随 A1 的 D5 批次一并补齐，三条奶茶已写入 `review_status: "pending"`。

| | |
|---|---|
| **现状** | ~~3 条缺字段~~ → **0 条**。三条奶茶（冰奶茶、去冰奶茶、热奶茶）已随 2026-09-17 的 D5 批次补上 `review_status`，现全表 147 条字段统一 |
| **成因** | 这三条是在 `patch_food_table.py` 那次三态迁移之后加入的，漏走了迁移 |
| **✅ 行为本来就正确，不是 bug** | `food_lookup.py:454-457` 有兼容回退：`status = "approved" if entry.get("reviewed") else "pending"`。这三条 `reviewed` 均为 `false` → 被正确当成 `pending` → 界面照常标「待验证」。实测 `patch_food_table.py --dry-run` 确认：迁移这 3 条后分布为**已通过 0 / 待审核 147 / 不通过 0** |
| **影响** | 原本无功能性影响，**仅数据字段不统一**。风险是以后有人写脚本只读 `review_status`、不做回退，就会漏掉这 3 条 —— 这正是后来补它的理由 |
| **怎么解的** | 在 `core/scripts/patch_food_table.py` 的 `ENTRY_FIXES` 里给这 3 条补 `review_status: "pending"`，再跑 `python scripts/patch_food_table.py`（数据改动走正规通道，幂等、强制 LF） |
| **验收标准** | 全表 147 条全部有合法 `review_status`（三态之一）。`core/tests/test_handover_docs.py::test_pending_e1_matches_actual_data_state` 已由「恰好 3 条缺字段」的**数据快照**改为**派生不变式**（有缺即红 + 概览状态必须已关） |

## E2　`herbs.json` 饮片条目缺逐条审核字段

> **状态：已解决（结构）（2026-09-20）。** 逐条字段已补齐，但**没有一条被审核** ——
> 逐条审核动作归 A1，本项只结掉「字段不存在」这个结构缺口。

| | |
|---|---|
| **现状** | ✅ **已补。** `herbs.json` 的饮片条目现在逐条带 `review_status`（三态）＋ `reviewed_by` / `reviewed_at` / `review_note`，当前**全部为 `pending`**、三个审核留痕键全为 `null`。插入位置在 `brewing` 之后，与 `food_properties.json` 的字段体系对齐；`_meta.version` 0.5.0 → **0.6.0** |
| **原本的问题** | 条目**完全没有逐条审核字段**，审核状态只存在于文件级 `_meta.review_status`。后果：① A1 的验收标准（每条 approved 都要有 `reviewed_by` + `reviewed_at`）对 herbs **无从落地**；② 有人写脚本按 `review_status` 过滤 herbs 会**全部落空且不报错**（静默失败） |
| **怎么解的** | **文本级插入**（不整文件回写——该文件是内联/展开混排，`json.dumps` 会炸出假 diff）：逐条做**大括号配平**定位条目收尾，在 `brewing` 收尾 `}` 前插 4 行并补逗号；幂等判据用**新串**（解析后每味都有 `review_status` 即跳过 —— ⚠️ **不能全文计数**：`_meta` / `constitution_extension` / `cautions_batch` / `rulings` / `evidence_batch` 里各自已有一处同名键）。脚本落在 gitignored 的 `core/var/`，未进版本库 |
| **拍板** | 只加 4 个字段，**不加** `reviewed` 布尔 —— 它是食物侧三态迁移的遗留，饮片侧**没有任何消费者**，出现了就是照着食物侧的表抄了一遍（守卫已把这条排除钉住）；影子字段 `_constitution_evidence` 本轮**不进** `herbs.json` |
| **⚠️ 一处必须说清的** | 本项**不打开 E7 的出口**。原方案曾写「随 E2 一并落地」，实测**不成立**：`safety.py` 的 `herb_evidence()` 只返回**本次实际用到的饮片**的来源，麦冬不在阴虚 `suitable_constitutions` ⇒ 永远不会进 `Basis.references`。E7 的出口是**渲染位**问题，不是数据位问题，已改记为 α／β 二选一 |
| **顺带** | `evidence_batch` 新增 `excluded` 段，把**地黄**的剔除理由落到数据里（鲜地黄/生地黄性寒、熟地黄性微温，四气方向相反，一条目录条目表达不了），免得日后被重复提议 |
| **验收标准** | ✅ **已达成**：每条饮片条目都有合法 `review_status`（三态之一）且四个字段齐备 —— 由 `core/tests/test_herb_review_fields.py` 守（派生不变式 + 5 条负控制 + 「至少看到一批饮片」防恒真）。其中一条负控制专门挡「把 `pending` 批量改成 `approved` 却没留 `reviewed_by` / `reviewed_at`」 |
| **相关文件** | `core/data/herbs.json`、`core/tests/test_herb_review_fields.py`（守卫）、`core/data/food_properties.json`（字段体系参照） |

## E3　饮片侧缺「逐味公开依据」链（分叉批次评估的缺口 3）

> 2026-09-18 评估同学的分叉批次时确认的本项目缺口之一。

| | |
|---|---|
| **现状** | ✅ **已实施（2026-09-18）。** 饮片侧现在有一份来源登记表 `core/data/herb_evidence_sources.json`（属性依据 44 味 + 体质适配依据 24 条 + 来源注册表 6 条），由 `core/scripts/build_herb_sources.py` 派生与校验，人读版渲染成 `docs/herb-evidence-sources.md`；`Basis.references` 已接到终端与网页 |
| **原本的问题** | **食物侧有、饮片侧没有。** `docs/food-properties-sources.json` 逐条登记了食性表的来源（原文 + 行号 + 依据等级），而 `herbs.json` 的 44 味**没有任何逐条来源字段** —— 每味的四气五味归经「依据是哪本书的哪一条」，代码与数据里都查不到 |
| **后果** | 用户与审核人只能看到结论（甘、平、归脾经），看不到出处。而这正是本项目**对食物侧已经做到**的事，两边口径不一致 |
| **可借的结构** | 分叉批次里有一套 `EvidenceReference` + 九型矩阵 + `Basis.references`，能在终端打印「淄博市卫健委《不同体质适合不同药膳》｜支持：生姜、枸杞子｜URL」。**只借结构，不借数据**（对方 `herbs.json` 未用于校验本项目的裁定）|
| **落地内容** | ① 登记表落 `core/data/`（**运行时读**，故不像 food 侧那样放 `docs/`——对齐的是结构不是位置）；② 矩阵 24 条**只进登记表、不写 `herbs.json`**（写进去会动 `filter_by_constitution` 的候选排序、并与 `rulings` 的成文职责语义混用）；③ `Basis` 加 `references`，由 `orchestrator._build_basis()` 这个**唯一构造点**装配（三处返回路径全走它），两个壳都呈现；④ `matcher.build_basis`（**无调用点的死代码**）已删除，并由源码守卫挡住复活 |
| **验收标准** | 结构化依据随每次推荐返回，且**推荐里出现的每一味都能在依据里找到对应** —— 这条端到端不变式是本项的核心验收（`core/tests/test_herb_evidence.py`），挡的正是「推荐了某味但引文里没有它」这类从输出表面看不出来的错 |
| **相关文件** | `core/data/herb_evidence_sources.json`、`core/scripts/build_herb_sources.py`、`docs/herb-evidence-sources.md`、`core/tests/test_herb_evidence.py`、`docs/food-properties-sources.json`（**结构参照**）、`docs/herb-evidence-chain-plan.md`（方案与拍板记录） |

**解决于** 2026-09-18，随提交「饮片侧逐味公开依据链（E3）主体」落地（`git log --oneline -- core/data/herb_evidence_sources.json` 查该提交 hash）。
同批留出两条待办：**C3**（药典 2020 版已废止，需换 2025 版）、**D3** 与本次无关但同属「饮片处理方式」族。
另如实记一条**本次不改数据**的发现：临沂页把**白扁豆**列入特禀质「应少食」，项目 `herbs.json` 里没有对应标注 —— 已写进登记表的 `_meta.known_limitations`，等所有者判断。

## E4　两条路径对**主导体质**的 `unsuitable_for` 处置不一致

> 2026-09-18 做 B2 接入（问卷结果接进推荐链路）时扫出来的**既有**不一致 ——
> **不是本次引入的**。按「不假装没看见」的原则单列登记；本次**不改**。

| | |
|---|---|
| **现状** | 同一味饮片若对**主导体质**标了 `unsuitable_for`，两条路径的处置不同：<br>**LLM 路径**（`core/app/domain/safety.py` 的 `filter_by_constitution`）：`continue` —— **硬排除**，饮片根本不进候选集；<br>**离线路径**（`core/app/services/matcher.py` 的 `fallback_recommend`）：只**追加一句 caution**，饮片仍在搭配里（软提示） |
| **后果** | 同一个用户，**有 Key 与没 Key 拿到的安全边界不同**，而界面只显示最终搭配，看不出差异来源 |
| **为什么本次没改** | ① 它是**产品口径**问题，不是实现疏漏：离线路径的契约是「永远给出合法且安全的搭配」，一律硬排除会出现「剔除后无搭配可用」的新分支要定；② 改它会牵动既有测试期望（`test_matcher` / `test_safety` 里对 cautions 的断言）。本次的边界是：**只**对**兼体质屏蔽集 `avoid`** 做硬剔除（决策 D6-A′），主导体质一律维持现状 |
| **为什么反过来不行（只改主导体质）** | 兼体质若两端不一致，就是「已知却不告知的不一致」——用户拿到的东西取决于有没有 Key，而没人告诉他 |
| **需要什么** | 定一条口径：离线路径是否也要对主导体质**硬排除**。<br>**(α) 是** → 复用本次已有的通用兜底分支（剔空退平和质通用搭配并留痕），但要评估默认搭配表 `CONSTITUTION_DEFAULT` 里有多少组会被剔空（剔空过多等于离线路径失去体质针对性）；<br>**(β) 否** → 在 `user_message` 里明写「离线结果的禁忌提示仅供参考」，否则是已知却不告知 |
| **验收标准** | ① 口径选定并落到代码；② 两条路径对**同一个 (体质, 饮片)** 给出同一种处置；③ `core/tests/test_constitution_integration.py` 的跨路径守卫扩展覆盖主导体质（目前它只守屏蔽集 `avoid`，正是因为这项目前不一致，守了就会红） |
| **相关文件** | `core/app/domain/safety.py`、`core/app/services/matcher.py`、`core/tests/test_constitution_integration.py`、`docs/b2-integration-execution-plan.md`（§0.2 发现记录与处置决定） |

## E5　非继承类 `how` 的 `matched_name` 缺守卫

> 2026-09-19 做 B2 时新增了 2 个 `how` 取值，但守卫只覆盖了其中之一。单列登记：
> 本轮边界是「本批 2 条守卫」，其余取值的 `matched_name` 覆盖属后续技术债 ——
> 不写下来，下一个人会以为「有 `how` 就有守卫」。

| | |
|---|---|
| **现状** | B2 给 `docs/food-properties-sources.json` 新增 `how` 取值 `制品·原料继承（语料原料）` 与 `同条目·品种方向描述`（`_meta.controlled_vocab.how` 由 7 项增至 9 项）。守卫**只覆盖前者**：`core/tests/test_food_review_sheet.py` 的「语料原料继承」守卫校验其 `matched_name` 必须在 `_meta.corpus_ancestors` 白名单内、且 `qi_mapped` 与白名单登记值相等（含负控制与变异检验）。`同条目·品种方向描述`（2 处：绿茶 = `cool`、红茶 = `warm`，同引《中医饮食营养学》行 566 的「茶叶」）**没有任何守卫** |
| **为什么本次没补** | 该取值的**设计前提**就是「同一语料条目内一凉一温」，单一取值的白名单**对它没有牙**（写 `cool` 则红茶红，写 `warm` 则绿茶红）。要补得先定「方向描述如何自动校验」，属新一类判据，不是照抄白名单 |
| **顺带记一条同一缺口** | `等价名（通称差异）` 也走语料等价关系，其 `matched_name` 同样无守卫。⚠️ 它**不是 B2 新增**取值（B2 之前就用于 `suan` → 大蒜），B2 只是多了 2 处引用（`baijiu` → 烧酒、`hongjiu` → 葡萄酒），共 3 处 |
| **需要什么** | 定这两类取值的 `matched_name` / `qi_mapped` 该怎么自动校验（白名单带方向？还是按 `locator` 行号反查语料？），再各配一条守卫 |
| **验收标准** | 两类取值的登记组合有守卫把关，且经**变异检验**（把被测数据改坏应变红、还原后回绿），样本从数据反推并断言「至少测到 N 组」（防样本被治理偷走后恒真） |
| **相关文件** | `docs/food-properties-sources.json`（`_meta.controlled_vocab.how`、`_meta.corpus_ancestors`）、`core/tests/test_food_review_sheet.py`（现有守卫）、`docs/food-properties-b2-staple.md`（§12.5 第 3 条） |

## E6　路线图文档的数字已失真

> 2026-09-19 做完 B5 时确认。它是一份**路线图**，失真会误导后来人排期 —— 故记进挂起项。

| | |
|---|---|
| **现状** | `docs/food-properties-remaining-plan.md` 的 §0 写「能引来源 35／无来源 65」、§1 写「L2 32 + L3 33」，在 B2–B5 之后**已全部失真**：实测已登记 **94/147**，B4 派生 `note` **53** 条，二者之和 = **147** = 全表 |
| **为什么值得单列** | 它是后来人**决定下一批做什么**的依据。它已两次给出错的方向：一是「33 条」的边界（真值 **19**），二是把「32」当成清单（实为 `100 − 35 − 33` 的**余数**，且 §1.2 只给了 16 条名单、其中 9 条同时出现在 L2 与 L3 名单里）|
| **实测现值（可直接替换）** | 登记表 `docs/food-properties-sources.json`：**94** 条（layer ①55／②21／③18）；B4 派生 `note`：**53** 条；「第一批」判据已改为按来源登记表的 `batch` 字段（33 条），不再按类别；核验单多出 §4.5／§4.6 两节 |
| **需要什么** | 按实测现值重写 §0／§1 的口径与数字，并把「上游给的清单是**余数**、不是名单」写成显式警告（否则后来人还会照着它排期）|
| **验收标准** | 文档数字与 `docs/food-properties-sources.json` 的实测值一致；「余数不是名单」这条教训留在文内 |
| **2026-09-21 进展** | 已在 `docs/food-properties-remaining-plan.md` 的 §0 与 §1 各加一条**注记**（照 `docs/agent2-9types-brew-plan.md` 的先例：历史方案**不改写正文**，只在旁边写清现值）。⚠️ 本条**仍未关闭**：注记只解决「别照它排期」，没解决「数字本身是错的」—— 真正结案要按上面的「需要什么」重写 §0／§1 |
| **相关文件** | `docs/food-properties-remaining-plan.md`、`docs/food-properties-sources.json`、`docs/food-properties-b5-l3.md` |

## E7　麦冬 × 阴虚：判定了，但没有用户可见出口

> 2026-09-20 做 34 味 × 9 型判定回填时产生。它是全批**唯一**一格「依据采纳了、却没落到任何
> 用户可见字段」的判定 —— 不单列就会被当成已结案（数据里确实有记录，但只是留痕）。

| | |
|---|---|
| **现状** | 34 味 × 9 型判定已回填（`core/data/herbs.json`，`_meta.version` 0.5.0 → **0.6.0**）。其中 **5.6 麦冬 × 阴虚**是 **E 级「宜」**（《中医养生学》:2287 药物养生清单直接点名「麦门冬」），按项目所有者裁定走 **A′：只记录、不入数组** —— 即**不**写入 `suitable_constitutions`，只记进 `_meta.constitution_extension.evidence_batch.recorded_only` |
| **为什么不写** | 写入会让麦冬（catalog 下标 8）**跃居阴虚候选第 1 位**（dry-run 实测：阴虚「宜」由 `桑椹` 变为 `麦冬 → 桑椹 → 酸枣仁 → 山药`）。这与 `rulings.priority_review` 里登记的山楂 × 气郁是同一类问题 —— 「写入 `suitable_constitutions` 即带升位效果」，而升位应由依据等级之外的因素决定 |
| **为何连 cautions 都走不了** | 「宜」类格子在现有守卫下**没有软通道**：<br>① caution 写含阴虚方向的文案 ⇒ `test_cautions_naming_yinxu_must_be_blocked` 规则②报红（命中阴虚关键词却既没硬屏蔽也没登记豁免）；<br>② 登记豁免 ⇒ 规则③ + `test_rulings_cells_are_well_formed` 双重报红 —— **`level=evidence` 只能写 `unsuitable_for`**，而把一个「宜」判定写成硬屏蔽是反向错误；<br>③ 把等级改成 `inference` 好让登记通过 ⇒ 那是**伪造依据等级**，不是豁免。<br>⇒ 这一格本轮**只能**是「记在数据里」。同理，5.4 酸枣仁、5.5 山药两格 E/E′ 级「宜」也是**只改数据、不登记 cells** |
| **阻塞什么** | 该格的结论**对用户不可见**：既不在候选排序里，也不在公开依据链里。它当前唯一的人类可读出口是 `herbs.json` 里那段 `recorded_only.decision` 文案。⚠️ 这不是「忘了写」，是**刻意暂缓** —— 但暂缓必须有留痕，否则将来没人知道这里欠一个出口 |
| **需要什么** | ⚠️ **不是「随 E2 落地」—— E2 已做完而本项仍开着。** 这是 2026-09-20 的**订正**：E2 补的是**数据位**（逐条审核字段），本项缺的是**渲染位**。`safety.py` 的 `herb_evidence()` 只返回**本次实际用到的饮片**的来源，而麦冬不在阴虚 `suitable_constitutions` ⇒ 进不了候选集 ⇒ 进不了 `Basis.references`；**把影子字段并进 `herbs.json` 也一样打不开它**（那只解决「数据放哪」，不解决「谁渲染」）。**二选一**：<br>**(α) 写进 `suitable_constitutions`、接受升位** —— 阴虚「宜」由 `桑椹` 变为 `麦冬 → 桑椹 → 酸枣仁 → 山药`，须照山楂 × 气郁的先例在 `rulings.priority_review` 同步登记升位疑虑；<br>**(β) 新增一个独立于推荐排序的依据渲染位** —— 让「判定了、但未进数组」的格子有正式出口（本项与将来的同类格子一并受益），代价是要动 `Basis` 的装配与两个壳的呈现 |
| **验收标准** | 二选一被明确选定；若选 (α)，`rulings.priority_review` 须同步登记升位疑虑；无论选哪个，`evidence_batch.recorded_only` 里这条**不得**静默消失（它是该格曾经被判定的唯一证据） |
| **相关文件** | `core/data/herbs.json`（`_meta.constitution_extension.evidence_batch`）、`core/tests/test_safety.py` |

## E8　昆布与食物表 `haili`（海带）跨库重复

> 2026-09-20 评估「扩展第一批 14 味」时产生。它是**跨库**重复（不是同一张表内的别名混入），
> 与 D4／D5／D17 同族但更棘手：两处都是**合法条目**，谁也不是「挂错」。

| | |
|---|---|
| **现状** | `core/data/food_properties.json` 已有 `haili`（海带，`nature: cool`），且其 `aliases` / `keywords` 里**精确含「昆布」**。而扩展评估要把「昆布」立成饮片条目 —— 一旦立成，同一个物在食物表与饮片表**各有一份取值**，将来任一侧调整都只改一处 |
| **为什么不在本轮直接决定** | 它牵动的不是「加不加一味」，而是**两表的分工口径**：食物表按「食材」收（海带是常见食材）、饮片表按「药食同源目录」收（昆布在 106 目录内），两者都成立。要么认「同一物只留一处」，要么认「两表各自成账、须互相标注」。这是口径决定，不是数据补录 |
| **需要什么** | 项目所有者拍板，三选一：<br>**(α) 不立饮片条目** —— 昆布的属性消费继续由食物表的 `haili` 承担，饮片侧不收录，本条关闭；<br>**(β) 两处并存但互相标注** —— 饮片条目与 `haili` 各加一条交叉引用说明（谁的取值为准、另一处为何保留）；<br>**(γ) 移除食物表里的「昆布」别名后立饮片条目** —— 与 A2 ② 同法（`aliases` 与 `keywords` **两处都要清**，`_find_entry` 第 2 级把两处合并扫描，只删一处无效） |
| **验收标准** | 三选一被明确选定并落到数据；「同一物在两表各有一份取值」这一状态不再**无声**存在 |
| **相关文件** | `core/data/food_properties.json`（`haili`）、`core/data/food_medicine_catalog.json`（106 目录，昆布在册）、`core/app/services/food_lookup.py`（`_find_entry` 合并扫描两处） |

## E9　化橘红与既有 `juhong`（橘红）近似名冲突

> 2026-09-20 评估「扩展第一批 14 味」时产生。与 **D6**（葱拆部位）、**D11**（寿司/咖喱拆子类）
> 不同族：它**不是粒度问题，是「另一个物取了近似的名字」**。

| | |
|---|---|
| **现状** | 饮片表已有 `juhong`（橘红，`herbs.json`）。候选「**化橘红**」是另一物 —— 基源为化州柚的未成熟外层果皮，而橘红为橘类果皮；且化橘红属 **2024 年第 4 号公告**新增的食药物质（同批还有地黄、麦冬、天冬），**带特殊人群使用限定** |
| **为什么不在本轮直接决定** | 两件事都没定：① 采名 —— 用户说「橘红」时该落到哪一条？两条并存会让匹配结果取决于 `keywords` 的扫描顺序；② 依据 —— 化橘红要作为独立条目，须有自己的属性与体质依据，**不能拿 `juhong` 的依据顶** |
| **需要什么** | 项目所有者拍板，二选一：<br>**(α) 不立条目** —— 认「橘红」为通称，化橘红走 `juhong`（须接受「以近似物的取值呈现另一物」的风险，并留痕）；<br>**(β) 立 `huajuhong` 独立条目** —— 须先取得两味各自的依据（药典/教材），并把两者在 `aliases` / `keywords` 上**互为排斥**，避免交叉命中 |
| **验收标准** | 二选一被明确选定；若选 (β)，须验证「说橘红」与「说化橘红」各自只命中一条，且两条的取值的依据分别可查 |
| **相关文件** | `core/data/herbs.json`（`juhong`）、`core/data/food_medicine_catalog.json`（2024 年第 4 号公告批次）、`core/app/domain/safety.py`（`herb_by_name` 名称索引） |

## E10　金银花的「做法依据」缺语料原句

> 2026-09-20 新增金银花（扩展第一批试点）时产生。来源：`core/data/herbs.json` 的
> `_meta.constitution_extension.evidence_batch.expansion_batch.open_questions` **第 1 条**。
> **做法**（是否须煎煮）是本项目饮片的必填口径之一 —— 不登记，这一格就会静默变成「已定」。

| | |
|---|---|
| **现状** | `jinyinhua` 的 `brewing` 里**不标** `requires_cooking`（即按「泡水代茶饮」处理）。判定链只有两条：① 形态为花蕾（轻清，不宜久煮）；② 《中药学》七版 1726「疏散风热、清泄里热以**生品为佳**」。但语料内**没有「泡服／代茶饮」的文献原句** —— 《中医饮食营养学》820 的「金银花露」是**蒸馏品**，不是冲泡 |
| **为什么本轮不直接定** | 守卫是**单边**的：`core/app/domain/safety.py` 只查「**标了** `requires_cooking` 却在处理要点里找不到「煮／煎／炖」依据」的条目；**不标**的那一侧没有守卫兜底。所以这一味「不标」时谁也不会拦它 —— 拿不到出处却仍按泡水呈现，等于**用形态推理冒充出处** |
| **需要什么** | 外部确认（具备资质的中医师/中药师）：金银花以**煎煮**还是**泡水**为准。若为泡水，请给可直接引用的语料出处（教材／药典用法项／权威科普），并**按项目的来源强度序**择优（药典 > 官方·审核词条 > 教材 > 科普 > 别名索引；科普只在药典与教材均无该条目时才可作基准） |
| **验收标准** | 二选一被明确选定并落到数据：① **有出处** ⇒ 补进 `core/data/herb_evidence_sources.json`（域 II 人工区）与该味 `brewing.note`；② **无出处** ⇒ 标 `brewing.requires_cooking: true`，并让 note 含「煮／煎／炖」。无论选哪个，`expansion_batch.open_questions` 里这一条**不得**静默消失（它是该判定曾被挂起的唯一证据） |
| **相关文件** | `core/data/herbs.json`（`jinyinhua.brewing`、`_meta.constitution_extension.evidence_batch`）、`core/data/herb_evidence_sources.json`、`core/app/domain/safety.py`（`herb_requires_cooking()`）、`core/app/agents/agent2_recommend.py`（「⚠️ 须煎煮」提示） |

## E11　金银花与既有凉／寒味饮片的湿热口径并存

> 同批次产生（`expansion_batch.open_questions` **第 2 条**）。**不是数据错，是两个口径同时在用** ——
> 不登记，后来人看到「同族五味标湿热、这一味不标」会当成漏填去「补上」。

| | |
|---|---|
| **现状** | 带 `damp_heat`（suitable）的饮片共 **14** 味；其中凉性、与金银花同族（清热类）的 **5 味**是菊花、决明子、淡竹叶、槐花、鱼腥草。本批按**现行判据**（粤中医《湿热质》宜／忌清单**未点名**金银花，网页已实测）把金银花判 `no_basis`（`suitable_constitutions` 留空）⇒ 二者并存 |
| **为什么本轮不直接定** | 那 5 味的 `damp_heat` 是**判据成文之前**的项目自拟（无「宜湿热」的原文支持），本批却改成了「须有文献点名」。两个方向都能辩护：**维持**（认它为历史判断、不动）或**另案补齐**（逐味找依据）。这是口径决定，不是补录 |
| **需要什么** | 项目所有者裁定，二选一：**(α) 维持并存** —— 承认判据成文前的自拟，把「同族饮片湿热口径不一致」这一事实留痕（本条即可关闭）；**(β) 另案补齐** —— 逐味为那 5 味找「宜湿热」的文献依据，找不到的改写口径。⚠️ 若选 (β)，**不要顺手把金银花「补」成湿热** —— 那是无依据地制造候选增量 |
| **验收标准** | 二选一被明确选定；「同族饮片对湿热质的口径不一致」不再**无声**存在（要么口径统一，要么差异在数据／文档里有登记） |
| **相关文件** | `core/data/herbs.json`（`jinyinhua` 与 `juhua`／`juemingzi`／`danzhuye`／`huaihua`／`yuxingcao` 的 `suitable_constitutions`）、`core/data/herb_evidence_sources.json`（域 II 体质依据）、`core/app/domain/safety.py`（`filter_by_constitution`） |

## E12　交接文档写「34 味食性表」（语义笔误，登记不改）

> 2026-09-20 做扩展第一批（34 → 35 味）同步计数时发现。属**既有**笔误、非本批引入。
> 与 **E6**（路线图文档数字失真）不同族：E6 是数字过时，这条是**把两张表说混了**。
> 本轮所有者指示**只登记、先不动**。

| | |
|---|---|
| **现状** | `docs/handover.md` §6 决策表第 2 条写「…双 Agent、**34 味食性表**、测试体系全部保留」。**语义本身是错的**：34（现 44）味指的是**饮片表** `core/data/herbs.json`，而**食性表**是 `core/data/food_properties.json` 的 **147** 条（129 食材 + 18 茶饮） |
| **为什么值得单列** | 交接文档是接手人的第一入口。把「饮片」写成「食性表」，会让人按 34/35 条去理解食性表的规模（实际 147 条）—— 而这正是 A1「147 条全部未人工审核」最容易被低估的地方 |
| **为什么本次不动** | 它在**决策表**里，那是一份「当时定了什么」的历史记录（与本批「历史文档不动」的裁定同族）。改与不改都会有人问，故先留痕、等所有者发话 |
| **需要什么** | 所有者裁定，二选一：**(α) 就地更正** —— 改为「饮片表（`herbs.json`）」，并顺查上下文有无同族误用；**(β) 维持不动** —— 认它是历史记录，改在别处补一句口径提醒。若选 (α)，注意 `handover.md` §6 的**决策编号连续性**由 `core/tests/test_handover_docs.py` 守着，只改措辞、不要动结构 |
| **验收标准** | 二选一被明确选定；改完后 `core/tests/test_handover_docs.py` 仍全绿（它只守结构、不守措辞） |
| **解决于** | **2026-09-20**（阶段 3 收尾·文档同步）：按 **(α) 就地更正** —— `handover.md` §6 决策 #2 的「34 味食性表」改为「**147 条食性表**」。§6 决策编号连续性未受影响（只改措辞），`test_handover_docs.py` 全绿 |
| **相关文件** | `docs/handover.md`（§6 决策表）、`core/data/herbs.json`、`core/data/food_properties.json`、`core/tests/test_handover_docs.py` |

## E13　`cautions` 与 `unsuitable_for` 的一致性守卫只覆盖阴虚方向

> 2026-09-20 做阶段 3 批三a（玉竹、黄精）时实测发现。属**既有**缺口、非本批引入 ——
> 本批两味的 `cautions` 都不含阴虚方向关键词，因此**不触发**任何断言，这正是缺口的体现。
> 所有者裁定：**只登记、先不补**（补守卫会立刻照出 E14 的数据问题，属另一层裁定）。

| | |
|---|---|
| **现状** | 全仓唯一的 cautions ↔ `unsuitable_for` 一致性守卫是 `core/tests/test_safety.py` 的 `YINXU_CAUTION_KEYWORDS`（＝阴虚／津液／口干／上火／内热）与纯函数 `_cautions_soft_gap`，**只覆盖阴虚方向**。阳虚／痰湿方向**没有对应守卫** —— 一味可以在 `cautions` 里写「脾胃虚寒者不宜」却不在 `unsuitable_for` 里屏蔽 `yang_deficiency`，测试不会报 |
| **为什么本轮不直接补** | 补守卫会立刻照出既有数据的不一致（见 **E14** 的 2 处），等于把一个「口径 + 数据」两层的裁定塞进一批文案改动里。批三a 的范围只是新增 2 味，不扩大 |
| **需要什么** | 项目所有者裁定，二选一：**(α) 补守卫 + 一并处置 E14** —— 按阳虚／痰湿方向各立一组关键词，把「命中却不屏蔽」的条目报出来，再逐条决定「补进 `unsuitable_for`」还是「改 `cautions` 措辞」；**(β) 维持现状** —— 承认守卫只守阴虚方向，在测试注释里写明覆盖面边界 |
| **验收标准** | 二选一被明确选定；「守卫只覆盖阴虚方向」不再**无声**存在（要么补上，要么在代码注释里写明边界） |
| **相关文件** | `core/tests/test_safety.py`、`core/data/herbs.json`（`_meta.constitution_extension.rulings` 的软通道登记）、`core/app/domain/safety.py` |

## E14　2 处 `cautions` 与 `unsuitable_for` 软硬脱节（青果×阳虚、红枣×痰湿）

> 2026-09-20 做阶段 3 批三a 时顺带实测出。属**既有**数据，非本批引入
> （`qingguo` 是批一落的，`hongzao` 更早）。所有者指示**只登记、先不改**。

| | |
|---|---|
| **现状** | 实测 2 处「`cautions` 提了某体质、`unsuitable_for` 却不管」：**① 青果 `qingguo`** —— `cautions` 写「性平偏寒，脾胃虚寒者不宜多饮」，但 `unsuitable_for` 是 **`[]`**（阳虚未硬屏蔽）；**② 红枣 `hongzao`** —— `cautions` 写「湿盛腹胀、痰湿偏重者不宜多食」，但 `unsuitable_for` 只有 **`["damp_heat"]`**（痰湿未硬屏蔽，挡的却是湿热）。对照：同族的麦冬／百合／龙眼肉都做到了「caution 提 → 字段也标」 |
| **为什么值得单列** | 条目内部自相矛盾：界面把 `cautions` 呈现给用户，`unsuitable_for` 却把该体质放进候选 —— 用户看到「不宜」的同时系统仍会推荐 |
| **为什么本次不动** | 改 `unsuitable_for` 会改候选集（可能把可见窗口里的其它味挤出去）⇒ 属**行为变更**，不是文案修正。按「先出方案、确认后再改」的项目约定留待裁定 |
| **需要什么** | 项目所有者裁定，逐条二选一：**(α) 补进 `unsuitable_for`**（与该 caution 取齐，改候选集）；**(β) 改 `cautions` 措辞**（把限定写软，与字段取齐）。⚠️ 若选 (α)，须跑候选快照对比（照 `core/var/b2_snapshot.py` 的做法），确认挤掉了谁 |
| **验收标准** | 这 2 处不再**无声**存在（要么字段与 caution 取齐，要么措辞与字段取齐） |
| **相关文件** | `core/data/herbs.json`（`qingguo`／`hongzao` 的 `cautions` 与 `unsuitable_for`）、`core/tests/test_safety.py`（E13 的守卫）、`core/app/domain/safety.py`（`filter_by_constitution`） |

## E15　离线路径把整句口述当 `note` 喂进温度通道（既串味、又换条目）

> 2026-09-20 做冲突框架评估时实测发现。属**既有** bug：Agent1 路径的同一问题早已修好
> （`agent1_diet.py` 注释里写明「曾经踩过」），**离线路径漏修**。所有者裁定**本轮即修**。

| | |
|---|---|
| **现状** | ✅ **已解决。** `core/app/api/analyze_offline.py` 原写作 `resolve_food(name=name, note=text)`，把**整句口述**当备注。是两个 bug 叠加：① 备注通道（去空格 ≤ `NOTE_MAX_LEN` = 10 字才生效）在**任意位置**找温度词 ⇒ 整句的「冰」算到了每样食物头上；② 备注通道剥掉温度词后的名字（「炸鸡可乐」）去查表，命中**「可乐」条目** ⇒ 「炸鸡」被按可乐算寒。**越短的输入越错**（长句超 10 字反而不走备注通道） |
| **修法** | 判定层零改动：`nature_math` 加「末尾温度词」识别 + 温度词须自成边界；`food_lookup` 加 `resolve_in_context(整句, 条目)`——只认领**紧贴本条目命中词之前**的温度词，走 name 通道。三道保护：已含温度词不重复叠加、不认领落在**别的已识别食物**命中范围内的温度词、拼出的名字必须仍指向同一条目。`resolve_food` 与备注通道**一行未动**（Agent1 仍在用） |
| **影响面** | 187 条输入（全表条目名 + 现实口述）**22 条变化，除「奶茶」重复命中一类外全是修正**：炸鸡／火锅／烧烤 寒→热、麻辣烫 平→热、「面条配冰镇啤酒」啤酒 凉→寒（长句原本丢冰） |
| **验证** | 新增 `core/tests/test_offline_segmentation.py`（35 项）：派生不变式 + 越界用例 + **三条负控制**（旧调用形状复现 bug、关掉位置规则丢冰、关掉交叉校验借「烫」）+ `ast` 结构守卫（离线路径不得出现 `resolve_food(note=…)`）。`pytest -q` **534 → 569 passed** |
| **解决于** | **2026-09-20**（commit `1902fb6`，未推送） |
| **相关文件** | `core/app/api/analyze_offline.py`、`core/app/services/food_lookup.py`、`core/app/domain/nature_math.py`、`core/tests/test_offline_segmentation.py` |

---

# 本次巡检的其他发现

以下几项不是「挂起事项」（已解决或已记录），但交接时值得知道：

| 项 | 结论 |
|---|---|
| 仓库根残留一个半成品 `.venv` | 已装发行版数约为 `core/.venv` 的 1/5，缺 pytest/fastapi/httpx/dotenv。**真正可用的是 `core/.venv`**。用错会报 `No module named pytest`。建议删掉根 `.venv`（绝对包数随安装而变，故不写死） |
| 全仓库无残留 API Key | 扫描 `sk-` 长串（排除 `.git`/`.venv`/`dsh-home`）**无命中**；`credentials.env` 只剩 `DSH_HOME` |
| `miniprogram/` 空目录已删除 | 转型遗留 |
| 三份生成型文档的测试已就位 | 分别有 18 / 23 / 26 项测试守着「层次不混淆」「快照不漂移」「差异不被静默改成一致」 |
| 交接文档自身也有测试 | 27 项，校验章节齐备、挂起项 ID 唯一且都有责任人与状态、引用的仓库路径真实存在、文档里没有 Key 材料 |
| 之前一处文档写错了影响面 | 参考数据曾写「四气差异会影响三层判定里的寒热加减」，经代码取证**是错的**，已更正（见 B1 的差异影响） |
| 维护文档的规模数字整体过时 | 已按 Python 口径重测更新（§1.4 与 §3 两张表）；顺带记录「别用 PowerShell 数行数」的陷阱 |
| 两个 venv 的 Python 都是 3.13.13 | 与 `pyproject.toml` 的 `requires-python = ">=3.10"` 兼容 |
| 屏蔽集可能把 LLM 候选集**整个清空** | 温阳方向的饮片常同时对阴虚质不宜，所以「阳虚质 + 屏蔽阴虚质」这类组合会让 `filter_by_constitution` 抛 `MissingConstitutionDataError`。**这是设计行为不是 bug**：编排层捕获后降级到离线路径，离线路径有通用兜底（已由 `core/tests/test_constitution_integration.py` 覆盖为「跳过该类组合」） |
| `NOTES.local.md` 已部分过期 | 仍写「尚无远程」「`miniprogram/` 空目录待删」「全部提交都是占位符身份」「pytest 230 项」，均与现状不符。它不入版本控制、不会随文档同步；已在 `docs/handover.md` §9.2 加了「以 `docs/` 为准」的提醒 |

---

## 更新约定

1. 任何一项**解决**后：更新本文的状态列，并在该项下补一行「**解决于** `<日期> <commit>`」。
2. 新增挂起项：按类别分配 ID（A 类对外阻碍 / B 类协作方 / C 类外部资料 / D 类决定 / E 类技术债）。
3. **本文是唯一真源**：`handover.md` 与 `maintenance.md` 只放摘要 + 指向本文的链接，不要在那儿复制明细。
4. 有测试校验本文的结构完整性（每个 ID 唯一、每项都有责任人与状态），改动后跑 `pytest -q`。

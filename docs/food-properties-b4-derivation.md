# B4 · L2 派生规则（6 条）· 成文与逐条推导

> 上游：`docs/food-properties-remaining-plan.md`（100 条 = L1 35 / L2 32 / L3 33 的分层方案）。
> 修订方案与拍板过程：`docs/food-properties-b4-derivation-plan.md`（含 6 个拍板点的备选与代价）。
> 本文是 **B4 第 1 步的产物**：把 6 条规则写成可执行的判据，并给出**逐条推导 + 逐条 note 文案**。
> 本步**不动任何数据**；数据改动（`_meta` 扩写、批量补 `note`）在所有者确认本文后执行。

---

## 0. 拍板记录（2026 年 9 月 19 日，所有者）

| 点 | 事 | 裁定 |
|---|---|---|
| **A** | 本批要不要动机制（`COOKING_DELTA`／`combine`） | **不动**。6 条规则全部落「数据 + 文档」；机制改动单列 **B4b**（§8） |
| **B** | L2 的范围 | **按判据**（不等 32）。实测 L2 = **53**（一致 49 + 冲突登记 4），退出 L2 → L3 2 条，归 L1 1 条 |
| **C** | 吐司/蛋糕的「烤」 | **轻度 +0**（定型烘焙）；高温/脱水烘烤（披萨/饼干）另判中度 +1 |
| **D** | 生食权重 | **甲案**：生食 -1、冰镇 -1，**同现时只计一次（取生食）**；不引入 -2，不引入分数权重 |
| **E** | 规则真源 | **以 `_meta.nature_change_rules`（数据）为单一真源**，代码与核验单脚本派生；加一致性守卫（§7 守卫 ④） |
| **F** | 「建议谨慎/建议观察」怎么给用户 | **写 `note`**（倾向性判断 + 建议）+ 冲突另做登记；食物侧新增 `cautions` 字段留到 B4b |

**额外确认**：① E 点的一致性守卫**必须覆盖** `stir_fried` 三份副本不一致（代码 0 / `_meta`「略偏温」/ 脚本 1）；② `combine()` 的「反向项 50% 抵消」与新规则「不抵消 + 冲突标记」的矛盾**记进 B4b，本批不改**；③ 挂起项 D11（寿司/咖喱拆条）**标注与既有裁定的冲突**（`shousi` 的 `review_note` 已裁定「去掉 raw 变体」）。

---

## 1. 六条规则（成文）

### R1 基底继承（打补丁版）

条目以某基础食材为主体时，**不能直接取该食材四气** —— 加工会改变基底的性味，须「基底 + 加工」联合定值。

- **判据**：基底必须在**表内**（或有已登记的四气）。基底不可得 ⇒ 本条**退出 L2**。
- 反例（说明补丁为什么必要）：`youtiao`（面平 → 成品热）、`shujiaotiao`（土豆平 → 热）、`bingqilin`（牛乳平 → 寒）—— 都是「未加工即基底值，加工后跨两档」。
- **复合主体**：主体不是单一食材的（火锅、咖喱、麻辣香锅、沙拉、果汁），基底记 **「复合（平起算）」**，不得拿「蔬菜」「水果」这类**表内不存在的类目**顶。

### R2 做法修正（方向性 ±1）

| 做法 | 增量 | 说明 |
|---|---|---|
| 生食 | **-1** | 甲案：与「冷藏/冰镇」**同现时只计一次**（取生食），不叠加、不引入 -2 |
| 冷藏/冰镇 | **-1** | 属 R3 的「轻·冷向」档 |
| 辛辣/重辣 | **+1** | |
| 热汤/烫煮 | **+1** | |
| 腌制 | **0** | **方向不确定**：不硬判，按 R6 在 `note` 里标「方向不定」 |

### R3 加工深度（档位登记表）

**同一加热强度分档，档位由做法本身决定，不得拿数据现值倒推**：

| 档 | 增量 | 做法 |
|---|---|---|
| **轻** | **+0** | 煮（蒸、卤、焯）；**定型烘焙**（吐司、蛋糕 —— 面点定型，含水高） |
| **中** | **+1** | 炒；明火烧烤；**高温/脱水烘烤**（披萨、饼干）；油糖酱焖烧（红烧）；久煮焖烧（火锅、麻辣香锅、咖喱）；卤汁浓酱 |
| **深** | **+2** | **油炸**（单次与双重同判）；**深度冷冻**（冷向 -2） |
| 轻·冷向 | -1 | 冷藏、冰镇 |
| 深·冷向 | -2 | 深度冷冻（冰淇淋） |

**「油炸为什么整档记深」的依据**（数据驱动，非口味判断）：表内 5 条油炸成品 **5/5** 相对基底跨两档 —— 面平→油条热、土豆平→薯条热、鱼平→炸鱼薯条热、鸡温→炸鸡热（夹取后仍为热）。单次与双次同判，故「双重油炸」不再额外叠档。

**本批 3 处档位判断（写在这里是为了被审，不是为了凑现值）**：

| 条目 | 本批判 | 备选 | 若判备选会怎样 |
|---|---|---|---|
| `pisa`、`binggan` | 高温/脱水烘烤 → **中 +1** | 与吐司同判「定型烘焙」轻 +0 | 两条立刻变**冲突**（推平 vs 现值温） |
| `hongshao` | 油糖酱焖烧 → **中 +1** | 「重油浓酱」判深 +2 | 立刻变**冲突**（推热 vs 现值温） |
| 沙拉、果汁 | 复合（平起算）＋生食 -1 → 凉 | 拿「蔬菜」「水果」类目当基底 | 违反 R1 判据（类目不在表内） |

### R4 发酵（新增维度）

| 情形 | 增量 |
|---|---|
| 酒／曲类发酵（啤酒、熟普后发酵） | **+1** |
| 乳发酵（奶茶、奶盖、酸奶） | **0** |
| 半发酵（乌龙茶） | **0** |
| 不确定 | 不硬判，按 R6 标「不确定」 |

### R5 多成分合成

主体成分为主，辅料只作 **±1 以内微调、不跨档**。

- **判据（本批修正）**：主体 = **占比最大的主食基底**，**不是** `combine()` 现用的「绝对值最大者」。
  - `niurou_hanbao`：按 `combine` 的「绝对值最大」→ 牛肉主导 → 温+烤 = **热**（✗）；按「主体=面包」→ 平+烤 = **温**（✓ 与现值一致）。
- 辅料不贡献增量时，`note` 记「配料 X（不跨档）」；**贡献增量时显式入算式**（`chadan`：配料茶叶（红茶温）+1）。

### R6 冲突标记与「不确定」包装

| 情形 | 处置 |
|---|---|
| 修正方向相反（啤酒：酒曲 +1 与冰镇 -1） | **不抵消**，标「属性冲突」，值不动，登记待裁定 |
| 依据不足（去冰奶茶、凉茶、生鱼片） | 给**倾向性判断 + 依据 + 建议**，值不动，登记待裁定 |
| 方向不确定（腌制） | `note` 里写「方向不定」，不硬判 |

**硬约束**：冲突条的 `note` **不得伪装成自洽**（不许写成「…所以是凉」）。必须出现「属性冲突」「项目值记 X」「待裁定」三要素 —— 这也正是守卫 ③ 的断言对象（§7）。

---

## 2. `note` 写作纪律（运行时硬约束）

`note` **不是注释，是运行时字段**：`resolve_temperature_fields(name, note)` 会扫它，命中温度词即给四气 ±1。实测条件（比函数注释更窄也更宽）：

- **窄**：只在 `len(note.replace(" ", "")) <= 10`（`NOTE_MAX_LEN`）时才扫 `note`；
- **宽**：`note` 通道的锚定判据是 `_find_entry(剥掉温度词后的余文)` 非空，而 `_find_entry` 第 1 级是**包含匹配** ⇒ 余文含任意条目名即可通过。

**B4 的两条纪律**：

1. **长度 ≥ 18 字（去空格）** —— 拟写的 53 条 `note` 实测最短 18 字，全部越过 `NOTE_MAX_LEN` ⇒ `note` 通道对它们恒不生效。
2. **不得写「冰镇/冰/加冰/去冰/冷冻/冷藏/冻」「热/烫/加热/温热」等前缀词** 作为可命中的短描述（长文案虽安全，仍避免把这些词当短语开头单列）。

### 顺带修好的一处既有副作用（实测）

`suannai_wan` 现有 `note`「冷藏酸奶加水果，偏凉」（去空格 10 字）**正好卡在上限**，于是：

| | 走哪条路 | 结果 |
|---|---|---|
| 不带 `note` | 查表直取 `酸奶碗` | 凉，置信度 **0.9**，detail「酸奶碗」 |
| 带现有 `note` | `note` 通道剥出「酸奶加水果，偏凉」→ 包含匹配命中 **`酸奶`** | 仍是凉，但置信度掉到 **0.6**，detail 变成「酸奶」 |

**四气没变，置信度与依据文案变了** —— 两个都是用户可见字段（`verification.detail` 用户可见）。B4 把这条 `note` 改成长文案后，`note` 通道不再触发，**该条回到 0.9 直取**。这是 B4 的一项实际收益，由守卫 ① 锁死。

---

## 3. 逐条推导（56 条候选）

判据（L2 准入）三条**同时**成立：① 基底可得；② 加工可表达（落在 §1 的规则表内）；③ **推得值 = 数据现值**。
第 ③ 条不成立 ⇒ 不是「可算」而是**属性冲突**，按 R6 处理（登记、值不动）。

| # | id | 名称 | 现值 | 推出 | 判定 | 推导 |
|---|---|---|---|---|---|---|
| 1 | `suantou` | 米粉 | 平 | 平 | 一致 | 基底 米（平）＋煮（蒸、卤）（档 轻，+0） |
| 2 | `youtiao` | 油条 | 热 | 热 | 一致 | 基底 面（平）＋油炸（档 深，+2） |
| 3 | `hanbao` | 汉堡 | 温 | 温 | 一致 | 基底 面（平）＋明火烧烤（档 中，+1） |
| 4 | `sanmingzhi` | 三明治 | 平 | 平 | 一致 | 基底 面（平）（无加工修正） |
| 5 | `yidali_mian` | 意大利面 | 平 | 平 | 一致 | 基底 面（平）＋煮（蒸、卤）（档 轻，+0） |
| 6 | `shousi` | 寿司 | 凉 | 凉 | 一致 | 基底 米（平）＋生食（-1） |
| 7 | `niurou_hanbao` | 牛肉汉堡 | 温 | 温 | 一致 | 基底 面（平）＋明火烧烤（档 中，+1） |
| 8 | `tusi` | 吐司 | 平 | 平 | 一致 | 基底 面（平）＋定型烘焙（档 轻，+0） |
| 9 | `sanmingzhi_huotui` | 火腿三明治 | 平 | 平 | 一致 | 基底 面（平）（无加工修正） |
| 10 | `mahuoguo` | 火锅 | 热 | 热 | 一致 | 基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1） |
| 11 | `malatang` | 麻辣烫 | 热 | 热 | 一致 | 基底 复合（平起算）＋辛辣（+1）＋热汤（烫煮）（+1） |
| 12 | `malahuoguo` | 麻辣香锅 | 热 | 热 | 一致 | 基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1） |
| 13 | `shaokao` | 烧烤 | 热 | 热 | 一致 | 基底 牛（温）＋明火烧烤（档 中，+1）＋辛辣（+1）（夹取） |
| 14 | `hongshao` | 红烧肉 | 温 | 温 | 一致 | 基底 猪（平）＋油糖酱焖烧（档 中，+1） |
| 15 | `lajiao_chao` | 辣炒菜 | 热 | 热 | 一致 | 基底 复合（平起算）＋炒（档 中，+1）＋辛辣（+1） |
| 16 | `zhajiang` | 炸酱面 | 温 | 温 | 一致 | 基底 面（平）＋炒（档 中，+1） |
| 17 | `guanzhudong` | 关东煮 | 温 | 温 | 一致 | 基底 复合（平起算）＋热汤（烫煮）（+1） |
| 18 | `shala` | 沙拉 | 凉 | 凉 | 一致 | 基底 复合（平起算）＋生食（-1） |
| 19 | `pisa` | 披萨 | 温 | 温 | 一致 | 基底 面（平）＋高温烘烤（档 中，+1） |
| 20 | `zhaji` | 炸鸡 | 热 | 热 | 一致 | 基底 鸡（温）＋油炸（档 深，+2）（夹取） |
| 21 | `shuijiao_rou` | 螺蛳粉 | 热 | 热 | 一致 | 基底 米（平）＋辛辣（+1）＋热汤（烫煮）（+1） |
| 22 | `huntun` | 馄饨 | 平 | 平 | 一致 | 基底 面（平）＋煮（蒸、卤）（档 轻，+0） |
| 23 | `hanshi_zhaji` | 韩式炸鸡 | 热 | 热 | 一致 | 基底 鸡（温）＋油炸（档 深，+2）＋辛辣（+1）（夹取） |
| 24 | `zhayu_shutiao` | 炸鱼薯条 | 热 | 热 | 一致 | 基底 鱼（平）＋油炸（档 深，+2） |
| 25 | `niuyouguo_shala` | 牛油果沙拉 | 凉 | 凉 | 一致 | 基底 复合（平起算）＋生食（-1） |
| 26 | `hanshi_banfan` | 韩式拌饭 | 温 | 温 | 一致 | 基底 米（平）＋辛辣（+1） |
| 27 | `rishi_lamian` | 日式拉面 | 温 | 温 | 一致 | 基底 面（平）＋热汤（烫煮）（+1） |
| 28 | `gali` | 咖喱 | 热 | 热 | 一致 | 基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1） |
| 29 | `kele` | 可乐 | 凉 | 凉 | 一致 | 基底 糖（平）＋冷藏（冰镇）（档 轻，-1） |
| 30 | `naicha` | 奶茶 | 平 | 平 | 一致 | 基底 茶（平）＋乳发酵（+0） |
| 31 | `suanmeitang` | 酸梅汤 | 平 | 平 | 一致 | 基底 糖（平）（无加工修正） |
| 32 | `guozhi` | 果汁 | 凉 | 凉 | 一致 | 基底 复合（平起算）＋生食（-1） |
| 33 | `bing_naicha` | 冰奶茶 | 凉 | 凉 | 一致 | 基底 茶（平）＋冷藏（冰镇）（档 轻，-1） |
| 34 | `qubing_naicha` | 去冰奶茶 | 凉 | 平 | **冲突** | 基底 茶（平）（无加工修正） |
| 35 | `re_naicha` | 热奶茶 | 温 | 温 | 一致 | 基底 茶（平）＋热汤（烫煮）（+1） |
| 36 | `kafei` | 咖啡 | 温 | — | 退出 L2 | 基底 咖啡豆（表内无此条目）＋定型烘焙（档 轻，+0） |
| 37 | `liangcha` | 凉茶 | 寒 | 凉 | **冲突** | 基底 复合（平起算）＋冷藏（冰镇）（档 轻，-1） |
| 38 | `pijiu` | 啤酒 | 凉 | 平 | **冲突** | 基底 大麦（平）＋酒曲发酵（+1）＋冷藏（冰镇）（档 轻，-1） |
| 39 | `dangao` | 蛋糕 | 平 | 平 | 一致 | 基底 面（平）＋定型烘焙（档 轻，+0） |
| 40 | `lvdougao` | 绿豆糕 | 凉 | 凉 | 一致 | 基底 绿豆（凉）＋煮（蒸、卤）（档 轻，+0） |
| 41 | `qiaokeli` | 巧克力 | 温 | — | 退出 L2 | 基底 可可（表内无此条目）（无加工修正） |
| 42 | `binggan` | 饼干 | 温 | 温 | 一致 | 基底 面（平）＋高温烘烤（档 中，+1） |
| 43 | `suannai_wan` | 酸奶碗 | 凉 | 凉 | 一致 | 基底 酸奶（平）＋冷藏（冰镇）（档 轻，-1） |
| 44 | `xila_suannai` | 希腊酸奶 | 凉 | 凉 | 一致 | 基底 酸奶（平）＋冷藏（冰镇）（档 轻，-1） |
| 45 | `bingqilin` | 冰淇淋 | 寒 | 寒 | 一致 | 基底 牛奶（平）＋深度冷冻（档 深，-2） |
| 46 | `shujiaotiao` | 薯条 | 热 | 热 | 一致 | 基底 土豆（平）＋油炸（档 深，+2） |
| 47 | `chadan` | 茶叶蛋 | 温 | 温 | 一致 | 基底 蛋（平）＋煮（蒸、卤）（档 轻，+0）＋配料茶叶（红茶温）（+1） |
| 48 | `lourou` | 卤肉 | 温 | 温 | 一致 | 基底 猪（平）＋卤汁浓酱（档 中，+1） |
| 49 | `huotui` | 火腿 | 温 | 温 | 一致 | 基底 猪（平）＋腌制（档 方向不定，+0）＋明火烧烤（档 中，+1） |
| 50 | `xiangchang` | 香肠 | 温 | 温 | 一致 | 基底 猪（平）＋腌制（档 方向不定，+0）＋明火烧烤（档 中，+1） |
| 51 | `shengyu` | 生鱼片 | 寒 | 凉 | **冲突** | 基底 鱼（平）＋生食（-1） |
| 52 | `puerg` | 普洱茶 | 温 | 温 | 一致 | 基底 茶（平）＋酒曲发酵（+1） |
| 53 | `wulong` | 乌龙茶 | 平 | 平 | 一致 | 基底 茶（平）＋半发酵（+0） |
| 54 | `damaicha` | 大麦茶 | 平 | 温 | 归 L1 | 基底 大麦（平）＋炒（档 中，+1）＋煮（蒸、卤）（档 轻，+0） |
| 55 | `putaoyou_cha` | 水果茶 | 凉 | 凉 | 一致 | 基底 茶（平）＋冷藏（冰镇）（档 轻，-1） |
| 56 | `naixicha` | 奶盖茶 | 平 | 平 | 一致 | 基底 茶（平）＋乳发酵（+0） |

**统计：候选 56 ｜ 一致 49 ｜ 冲突 4 ｜ 退出 L2 2 ｜ 归 L1 1。**
⇒ **本批 L2 = 53 条**（一致 49 写自洽 `note`；冲突 4 写冲突 `note` + 登记）；`kafei`／`qiaokeli` 落 **L3 ③**；`damaicha` 归 **L1**（语料有据）。

### 与拍板 B 的关系

上游的「32 条」是 **100 - 35 - 33 的余数**，不是清单（上游只给了 16 条名字，另 16 条全文没写名字；且 §1.3③-a 写「13 条」而实列 14 个名字）。本批按判据实算得 **53 条** —— 与方案 §1.4 的预估（≈47–55）一致。

---

## 4. 拟写 `note` 清单（确认稿）

### 4.1 一致项（49 条，写自洽派生式）

| id | 名称 | 拟写 `note` |
|---|---|---|
| `suantou` | 米粉 | 派生：基底 米（平）＋煮（蒸、卤）（档 轻，+0）＝平 |
| `youtiao` | 油条 | 派生：基底 面（平）＋油炸（档 深，+2）＝热 |
| `hanbao` | 汉堡 | 派生：基底 面（平）＋明火烧烤（档 中，+1）＝温；配料 牛肉（不跨档） |
| `sanmingzhi` | 三明治 | 派生：基底 面（平）（无加工修正）＝平；配料 火腿（不跨档） |
| `yidali_mian` | 意大利面 | 派生：基底 面（平）＋煮（蒸、卤）（档 轻，+0）＝平 |
| `shousi` | 寿司 | 派生：基底 米（平）＋生食（-1）＝凉；配料 醋（调味，不抵消） |
| `niurou_hanbao` | 牛肉汉堡 | 派生：基底 面（平）＋明火烧烤（档 中，+1）＝温；配料 牛肉（不跨档） |
| `tusi` | 吐司 | 派生：基底 面（平）＋定型烘焙（档 轻，+0）＝平 |
| `sanmingzhi_huotui` | 火腿三明治 | 派生：基底 面（平）（无加工修正）＝平；配料 火腿（不跨档） |
| `mahuoguo` | 火锅 | 派生：基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1）＝热 |
| `malatang` | 麻辣烫 | 派生：基底 复合（平起算）＋辛辣（+1）＋热汤（烫煮）（+1）＝热 |
| `malahuoguo` | 麻辣香锅 | 派生：基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1）＝热 |
| `shaokao` | 烧烤 | 派生：基底 牛（温）＋明火烧烤（档 中，+1）＋辛辣（+1）＝热 |
| `hongshao` | 红烧肉 | 派生：基底 猪（平）＋油糖酱焖烧（档 中，+1）＝温 |
| `lajiao_chao` | 辣炒菜 | 派生：基底 复合（平起算）＋炒（档 中，+1）＋辛辣（+1）＝热 |
| `zhajiang` | 炸酱面 | 派生：基底 面（平）＋炒（档 中，+1）＝温 |
| `guanzhudong` | 关东煮 | 派生：基底 复合（平起算）＋热汤（烫煮）（+1）＝温 |
| `shala` | 沙拉 | 派生：基底 复合（平起算）＋生食（-1）＝凉 |
| `pisa` | 披萨 | 派生：基底 面（平）＋高温烘烤（档 中，+1）＝温；配料 奶酪（不跨档） |
| `zhaji` | 炸鸡 | 派生：基底 鸡（温）＋油炸（档 深，+2）＝热 |
| `shuijiao_rou` | 螺蛳粉 | 派生：基底 米（平）＋辛辣（+1）＋热汤（烫煮）（+1）＝热 |
| `huntun` | 馄饨 | 派生：基底 面（平）＋煮（蒸、卤）（档 轻，+0）＝平 |
| `hanshi_zhaji` | 韩式炸鸡 | 派生：基底 鸡（温）＋油炸（档 深，+2）＋辛辣（+1）＝热 |
| `zhayu_shutiao` | 炸鱼薯条 | 派生：基底 鱼（平）＋油炸（档 深，+2）＝热 |
| `niuyouguo_shala` | 牛油果沙拉 | 派生：基底 复合（平起算）＋生食（-1）＝凉；配料 牛油果（平） |
| `hanshi_banfan` | 韩式拌饭 | 派生：基底 米（平）＋辛辣（+1）＝温 |
| `rishi_lamian` | 日式拉面 | 派生：基底 面（平）＋热汤（烫煮）（+1）＝温 |
| `gali` | 咖喱 | 派生：基底 复合（平起算）＋辛辣（+1）＋久煮焖烧（档 中，+1）＝热 |
| `kele` | 可乐 | 派生：基底 糖（平）＋冷藏（冰镇）（档 轻，-1）＝凉 |
| `naicha` | 奶茶 | 派生：基底 茶（平）＋乳发酵（+0）＝平；配料 奶（不跨档） |
| `suanmeitang` | 酸梅汤 | 派生：基底 糖（平）（无加工修正）＝平；配料 乌梅（表值平） |
| `guozhi` | 果汁 | 派生：基底 复合（平起算）＋生食（-1）＝凉 |
| `bing_naicha` | 冰奶茶 | 派生：基底 茶（平）＋冷藏（冰镇）（档 轻，-1）＝凉 |
| `re_naicha` | 热奶茶 | 派生：基底 茶（平）＋热汤（烫煮）（+1）＝温 |
| `dangao` | 蛋糕 | 派生：基底 面（平）＋定型烘焙（档 轻，+0）＝平；配料 蛋、糖（不跨档） |
| `lvdougao` | 绿豆糕 | 派生：基底 绿豆（凉）＋煮（蒸、卤）（档 轻，+0）＝凉 |
| `binggan` | 饼干 | 派生：基底 面（平）＋高温烘烤（档 中，+1）＝温 |
| `suannai_wan` | 酸奶碗 | 派生：基底 酸奶（平）＋冷藏（冰镇）（档 轻，-1）＝凉；配料 水果（不跨档） |
| `xila_suannai` | 希腊酸奶 | 派生：基底 酸奶（平）＋冷藏（冰镇）（档 轻，-1）＝凉 |
| `bingqilin` | 冰淇淋 | 派生：基底 牛奶（平）＋深度冷冻（档 深，-2）＝寒；配料 糖（不跨档） |
| `shujiaotiao` | 薯条 | 派生：基底 土豆（平）＋油炸（档 深，+2）＝热 |
| `chadan` | 茶叶蛋 | 派生：基底 蛋（平）＋煮（蒸、卤）（档 轻，+0）＋配料茶叶（红茶温）（+1）＝温 |
| `lourou` | 卤肉 | 派生：基底 猪（平）＋卤汁浓酱（档 中，+1）＝温 |
| `huotui` | 火腿 | 派生：基底 猪（平）＋腌制（档 方向不定，+0）＋明火烧烤（档 中，+1）＝温 |
| `xiangchang` | 香肠 | 派生：基底 猪（平）＋腌制（档 方向不定，+0）＋明火烧烤（档 中，+1）＝温 |
| `puerg` | 普洱茶 | 派生：基底 茶（平）＋酒曲发酵（+1）＝温 |
| `wulong` | 乌龙茶 | 派生：基底 茶（平）＋半发酵（+0）＝平 |
| `putaoyou_cha` | 水果茶 | 派生：基底 茶（平）＋冷藏（冰镇）（档 轻，-1）＝凉；配料 水果（不跨档） |
| `naixicha` | 奶盖茶 | 派生：基底 茶（平）＋乳发酵（+0）＝平；配料 奶盖（不跨档） |

> 覆盖关系：`shousi`／`xila_suannai` 等**已有 `note`／`review_note` 的条目会被本条改写**；改写只动 `note`，**`review_note` 与 `nature` 一律不动**（`review_note` 是审核侧留痕，与本批无关）。

### 4.2 冲突项（4 条，写冲突式 + 必须登记）

| id | 名称 | 现值 → 推出 | 拟写 `note` |
|---|---|---|---|
| `qubing_naicha` | 去冰奶茶 | 凉 → 平 | 属性冲突：基底 茶（平）（无加工修正）＝平，项目值记凉。去冰后按规则应回平，但项目保留了「去冰仍比热饮偏凉」的既有裁定 ⇒ 按 R6 标记为属性冲突、保留项目值，待裁定。 |
| `liangcha` | 凉茶 | 寒 → 凉 | 属性冲突：基底 复合（平起算）＋冷藏（冰镇）（档 轻，-1）＝凉，项目值记寒。草本基底（凉茶草药方）在表内无条目，现值按草本药性记寒 ⇒ 按 R6 标记为属性冲突、保留项目值，待裁定。 |
| `pijiu` | 啤酒 | 凉 → 平 | 属性冲突：基底 大麦（平）＋酒曲发酵（+1）＋冷藏（冰镇）（档 轻，-1）＝平，项目值记凉。酒曲发酵（+1）与冰镇（-1）方向相反 —— 正是 R6 要标记的「不抵消」场景 ⇒ 按 R6 标记为属性冲突、保留项目值，待裁定。 |
| `shengyu` | 生鱼片 | 寒 → 凉 | 属性冲突：基底 鱼（平）＋生食（-1）＝凉，项目值记寒。生食只到 -1（档位不足），现值按「生鱼偏寒」记寒；此为拍板 D 取甲的代价 ⇒ 按 R6 标记为属性冲突、保留项目值，待裁定。 |

---

## 5. 冲突登记放哪（**待所有者拍板的一处偏离**）

方案 §5.3 守卫 ③ 原写「判为冲突的条目在 `sources.json` 里必须有 `open_question`」。**实测这 4 条都不在来源登记表里**（登记表 75 条，全部带 `evidence[]`；`pijiu`／`liangcha`／`qubing_naicha`／`shengyu` 均无条目）。

于是有两条路：

| | 做法 | 代价 |
|---|---|---|
| **甲（本批建议）** | 冲突登记放**数据侧**：`core/data/food_properties.json` 的 `_meta.derivation_conflicts`（与 `_meta.nature_change_rules` 同处 —— 拍板 E 已定「`_meta` 是规则单一真源」） | 核验单不直接渲染该块；**实测它也**不渲染冲突 `note`（「派生：」「属性冲突」在核验单里各出现 0 次）⇒ 人类可读出口＝**本文档 §4** 与 `_meta.derivation_conflicts`。见 §10「事实更正 2」 |
| 乙 | 往 `sources.json` 加 4 条（`batch=后续`、`status` 取 ③ 类、`reference=null`、`evidence=[]`、带 `open_question`） | 触发该表 12 条硬约束（词表登记、三态计数实算、`counts` 自洽、layer 映射…）+ 重生成核验单；且**来源登记表里放「无来源」的条目**语义别扭 |

**建议甲**：这 4 条的冲突是**派生口径冲突**，不是来源冲突；`sources.json` 的职责是「这个值从哪来」，而 L2 条目按定义**没有直接来源**（是推出来的）。

---

## 6. 退出 L2 / 归 L1（3 条）

| id | 名称 | 处置 | 依据 |
|---|---|---|---|
| `kafei` | 咖啡 | **退出 L2**（落 L3 ③） | 基底「咖啡豆」表内无条目。现值温、且已有 `variant_nature{cold: neutral}`（温 + 冰镇 -1 = 平）—— 变体算式与 R2 一致，但基底值无处可推 |
| `qiaokeli` | 巧克力 | **退出 L2**（落 L3 ③） | 基底「可可」表内无条目 |
| `damaicha` | 大麦茶 | **归 L1** | 语料（《本草纲目》「大麦」条）有据 ⇒ 属「能引来源」的 L1，不走派生；本批不动 |

---

## 7. 已实现的守卫（`core/tests/test_b4_derivation_notes.py`，12 条测试）

设计意图与落地形态有**两处偏离**（拍板 A 的必然结果），已在 §10 逐条交代。

| 守卫 | 断言 | 负控制（变异必红） |
|---|---|---|
| **①-a 全表 `note` 无温度副作用** | 遍历 147 条：`resolve_temperature_fields(name, note)` 不得返回 `from_field == "note"` | 把 `kele` 的 note 改成 `冰镇可乐`（≤10 字）⇒ 必红；合成样本 `naicha + note="冰镇"` 必须**被检出**（防断言恒真）；另断言「全表 ≥147 条」防样本被偷 |
| **①-b 派生类 `note` ≥18 字** | 53 条「派生：／属性冲突：」note 去空格 ≥18 字（越过 `NOTE_MAX_LEN` ⇒ 通道恒不生效） | 把 `suantou` 的 note 改短 ⇒ 必红 |
| **①-c 短 note 白名单冻结** | 全表 `≤ NOTE_MAX_LEN` 的 note 只允许是登记在案的 13 条旧茶饮；**白名单腐烂（条目变长/改名）也红** | 给 `tusi` 写一条 3 字 note ⇒ 必红 |
| **② 一致项 `note` 自洽** | 49 条：`note` 里最后一个「＝X」必须等于该条 `nature` | 把 `suantou` 的末值 `＝平` 改成 `＝寒` ⇒ 必红 |
| **③ 冲突项「登记 ⟷ `note`」双向一致** | 4 条：`_meta.derivation_conflicts` 有登记、`note` 含「属性冲突」「待裁定」、**不含**「＝<现值>」；双向对称（多一条少一条都红） | 删掉 `pijiu` 登记 ⇒ 必红；把 `pijiu` 的冲突 `note` 改写成自洽式 ⇒ 必红 |
| **④ 规则单一真源（声明式绑定）** | `_meta.runtime_delta` ⟷ `nature_math.COOKING_DELTA` **硬等值**；`_meta.script_delta` ⟷ 核验单脚本 `LAYER2_DELTA` **硬等值**；canonical 与 runtime 的差集必须**逐条登记**在 `_meta.alignment_pending`，且登记值要与实值相符 | 改 `_meta.runtime_delta.stir_fried` ⇒ 必红；**改代码 `COOKING_DELTA`** ⇒ 必红；在 canonical 造一处未登记的差异 ⇒ 必红 |
| **⑤ 数据 `note` ⟷ 文档 §4 逐字** | 本文档 §4 的 53 条与数据 `note` 逐字一致（审核依据不得与事实脱钩） | 改文档里任一条 ⇒ 必红 |

**变异检验结果：10/10 条如预期变红**，还原后 12 条守卫全绿。

守卫 ① 是**把现有仅守 `jiangyou` 一条升级为派生不变式**；①-a 的通过同时意味着 `suannai_wan` 的 `note` 通道副作用已被消除（§2）。

---

## 8. 留给 B4b（机制批次）的清单

1. **`combine()` 的两条判据**：① 「反向项 50% 抵消」 vs 新规则「不抵消 + 冲突标记」；② 「主导项=绝对值最大」 vs L2 口径「主体=占比最大的主食基底」。⇒ 与 `combine()` 接入主流程一并定（当前它**没有调用点**，故本批不改）。
2. **`COOKING_DELTA` 与 `_meta` 的派生方向**：拍板 E 定了「`_meta` 为真源」，B4b 要把代码与脚本改成**从 `_meta` 派生**，并把 `raw` 的语义（代码 0 vs 文案「生冷类偏寒」）一次性对齐。
3. **食物侧 `cautions` 字段**（拍板 F 的乙案）：新增字段 + 接进 `Recommendation.cautions`；需确认不会串到 `herbs.json` 饮片的 `cautions` 语义（`matcher.py` 读的是 `entry.get("cautions")`）。
4. **挂起项 D11**（寿司/咖喱拆条）标注冲突：`shousi` 的 `review_note` 已裁定「去掉 raw 变体」，拆条会把「生食款」重新变回待决问题。

---

## 9. 执行顺序（本批）

1. **本文**（规则成文，不含数据改动）—— 完成，待确认。
2. `_meta.nature_change_rules` 扩写（R2/R3/R4 档位表）＋ `_meta.derivation_conflicts`（4 条冲突登记）。
3. 逐条补 `note`（走 `patch_food_table.py`，幂等；**只动 `note`，`nature` 一律不动**）。
4. 重生成核验单（脚本产物，必须最后生成）。
5. 4 条守卫 + 变异检验。

**明确不做**：不改任何 `nature`（含 4 条冲突）；不改 `herbs.json`；不改 `.env`；不改运行时判定代码；不给条目写 `approved`（A1 仍是 ⛔）；不复制语料文件进仓库；不动挂起项 A1／E4／D4–D6 与 D11 的实体内容（只在挂起项里标注冲突）。

---

## 10. 执行记录（第 2–5 步已完成）

§9 的 1–5 步全部落地。**数据侧零 `nature` 改动**：`git diff` 的删除行里不含 `nature`／`variant_nature`／`review_note`／`review_status`（逐行核对）。

| 落点 | 内容 |
|---|---|
| `core/data/food_properties.json` | 53 条 `note`（20 条为覆盖旧 gloss）＋ `_meta.nature_derivation_rules` ＋ `_meta.derivation_conflicts` |
| `core/scripts/patch_food_table.py` | `LAYER2_NOTES`（53 条，内容区自动生成自本文档 §4）＋ `META_PATCH`；**唯一写入者**，幂等，强制 LF |
| `core/tests/test_b4_derivation_notes.py` | 12 条测试（6 组守卫，见 §7） |
| `docs/food-properties-review-sheet.md` | 重生成后**内容零变化**（它不渲染 `note`），`--check` 通过 |

### 偏离 1：规则真源落**新键**，不动 `nature_change_rules`

`_meta.nature_change_rules` 是**散文**，且经 `render_nature_change_rules()` **渲染进 Agent1 提示词**（`agent1_diet.py`）。而 B4 的档位与当前代码 `COOKING_DELTA` 在**三处**不同（油炸 深+2 / 生食 -1 / 炒 +1）。把 B4 口径写进提示词，等于把「文档口径」当「运行时行为」讲给模型 —— 那正是拍板 A（本批不动机制）要避免的。

⇒ 机器可读的规则表落在**新键** `_meta.nature_derivation_rules`（**不渲染进提示词**），供守卫 ④ 与 B4b 收口使用。

### 偏离 2：守卫 ④ 是「声明式绑定」，不是「三份取值一致」

拍板 A 不动机制 ⇒ B4 口径与代码**必然**有差（`deep_fried`／`raw`／`stir_fried` 三条）。断言一个当下为假的"一致"没有意义。故 ④ 写成：

- `_meta.runtime_delta` ⟷ `nature_math.COOKING_DELTA` **硬等值**；
- `_meta.script_delta` ⟷ 核验单脚本 `LAYER2_DELTA` **硬等值**；
- canonical（B4 口径）与 runtime 的差集必须**逐条登记**在 `_meta.alignment_pending`，**未登记的差异一律变红**。

⇒ 差异**被声明、可数、可收口**，而不是静默共存；任一侧漂移（改代码不改数据、或反之）立即报红。

### 偏离 3（事实更正）：「全表 `note` ≥18 字」做不到

全表有 **13 条旧茶饮条目**的 `note` 只有 **4–9 字**（菊花茶、枸杞茶、绿茶、红茶……，**本批之前就存在**）。它们 ⇒ 都 ≤ `NOTE_MAX_LEN`，note 通道对它们是**开着的**；实测它们**恰好**都不含温度前缀，所以现在没有副作用 —— 但形态脆弱。

⇒ 守卫 ① 拆成三条（§7）：全表「无 note 通道命中」＋ 派生类「≥18 字」＋ **短 note 白名单**（13 条点名登记在测试里，**新增短 note 即红、白名单腐烂也红**）。把脆弱面冻结并可见，好过断言一个为假的"全表安全"。

**待所有者定**：要不要顺手把这 13 条茶饮的 `note` 也补长（属 L1/L3 条目、超出 B4 范围，会同时改动它们进提示词的那句「说明：…」）？

### 事实更正 2：冲突 `note` 在核验单里**也不可见**

§5 甲案原写「审核人**看得到**」—— 实测**不成立**：核验单只渲染 `§4.2` 那 4 条既有 note（「口径分歧」出现 4 次），「派生：」「属性冲突」在 508 行核验单里**各出现 0 次**。B4 的 53 条 note 与 4 条冲突 note 的人类可读出口只有**本文档 §4** 与提示词里的「说明：…」。

⇒ **建议单列下一批**：给核验单新增一节渲染 `_meta.derivation_conflicts`（属核验单脚本改动，不塞进本批）。

### 附带修正（既有守卫的过宽断言）

`test_food_lookup.py::test_fried_chicken_resolves_to_its_own_entry` 原先断言渲染结果里**不含裸词**「油炸」。B4 给 `zhaji` 写了它**自己**的派生 note（「…基底 鸡（温）＋油炸（档 深，+2）＝热」），裸词断言会把这条自述误判成「`jirou` 的变体又漏出来了」。已收紧为**变体行形态** `油炸→` 与 `（油炸` —— 原意图（别名污染不回归）一字未改。

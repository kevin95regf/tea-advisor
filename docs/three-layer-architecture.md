# 三层混合判断架构

食物寒热属性的判定分三层，置信度递减，来源可追溯。

```
用户口述
   ↓
┌─────────────────────────────────────────────┐
│ 第一层 硬规则库          confidence 0.9      │
│   food_properties.json 规范名精确命中         │
│   直接查表，表值即权威，不叠加模型猜测         │
├─────────────────────────────────────────────┤
│ 第一层附 食材变体层       confidence 0.9      │
│   条目自带 variant_nature（如 红薯→烤红薯 温）│
│   查表直取，优先于通用烹饪修正                 │
├─────────────────────────────────────────────┤
│ 第二层 组合推理（烹饪修正） confidence 0.6    │
│   未精确命中规范名时才生效                     │
│   蒸煮 0 / 煎炸 +1 / 烧烤 +1 / 辛辣 +1 / 冰镇 -1│
├─────────────────────────────────────────────┤
│ 第二层附 温度前缀层       confidence 0.6      │
│   「冰啤酒」「热牛奶」的冰/热用**纯字符串规则**识别 │
│   剥掉前缀后须是完整表内食物名才生效            │
├─────────────────────────────────────────────┤
│ 第三层 LLM 推测          confidence 0.3      │
│   表里没有 → 保留模型判断但标记未验证          │
│   完全判不出 → unresolved / 0.1              │
└─────────────────────────────────────────────┘
   ↓
逐项写入 verification{source, confidence, unverified, detail}
   ↓
接口返回 → 前端按阈值决定是否显示、是否标注
```

## 已实现的文件

| 文件 | 职责 |
|---|---|
| `app/domain/nature_math.py` | 四性数值编码、夹取、两层修正、合成算法 |
| `app/services/food_lookup.py` | 匹配、`resolve_food()` 确定性判定 |
| `app/agents/agent1_diet.py::calibrate_parsed` | 用判定结果覆盖/标注模型输出 |
| `app/agents/agent2_recommend.py::_build_unverified_section` | 把未验证项隔离给推荐器 |
| `data/food_properties.json` | 143 条食性数据 + 逐条审核状态 |
| `miniprogram/utils/format.js` | 按置信度决定显示与标注 |

## 四性数值编码

```
寒 = -2   凉 = -1   平 = 0   温 = +1   热 = +2
```

溢出夹取：性热的羊肉再油炸仍是热（`clamp` 到 +2），不会出现"超热"。

**unknown 没有值（返回 `None`），不是 0**——否则"未判定"会被当成平性参与运算。

## 两层修正的顺序（不可颠倒）

1. **食材变体层**：条目自带的 `variant_nature` 直接取值，如「可乐」在 `cold` 下为「寒」。
2. **烹饪修正层**：仅当名称**未精确命中**表内规范名时生效，±1 兜底。

为什么不能颠倒：表里精确的变体值会被通用规则破坏。

**为什么"精确命中"要比较名称整体而不是命中词**：`炸馒头` 靠关键词"馒头"识别、
`冰啤酒` 靠包含关系识别，它们的名称与规范名不同，属于"带烹饪信息的派生名称"，
应当让烹饪层生效；而 `希腊酸奶` 名称与规范名完全相同，表值即权威。

## 第二层合成算法（多样食材）

规则由使用者确认，实现在 `nature_math.combine`：

1. **取绝对值最大者为主导项**；同向项累加，**封顶 ±2**；
2. **反向项只做 50% 抵消，不翻转主导方向**（`dominant + 0.5×反向和`，再按主导方向夹到同号）；
3. 平性（0）既不贡献方向也不稀释——加米饭不会把凉性拉平；
4. 并列时取先出现的（通常是主料），保证同样输入结果稳定；
5. **烹饪修正最后叠加**。

示例：
- `番茄(-1) + 鸡蛋(0) + 炒(0)` → -1 → **凉**
- `羊肉(+2) + 苦瓜(-2)` → 2 + 0.5×(-2) = 1 → **温**（不翻转成寒凉）
- `凉(-1) + 温(+1)` → -1 + 0.5 = -0.5 → **凉**（不翻转）

## 温度前缀层（纯规则，不经过模型）

**为什么单独做一层**：模型给的 `cooking` 无法区分"可靠证据"与"猜测"——
它会给出「希腊酸奶=cold」「火腿三明治=grilled」这类没有依据的值。
而「冰啤酒」「热牛奶」里的"冰/热"本身就在名字里，是字符串层面的确定信号。

```
识别前缀（CHILL_PREFIXES / HEAT_PREFIXES，长前缀优先）
   ↓
剥掉前缀 → 净名字
   ↓
准入条件：净名字必须**恰好等于**某个条目的规范名或别名
   ↓ 通过
净名字查表拿表值 → 叠加温度增量（shift_nature 自动夹取）
   ↓ 不通过
丢弃温度信息，按原名正常查表
```

**准入条件为什么必要**（这是本层最关键的安全边界）：

| 输入 | 剥后 | 是完整食物名？ | 结果 |
|---|---|---|---|
| 冰啤酒 | 啤酒 | ✅ 表内条目 | 凉 -1 → **寒** |
| 热牛奶 | 牛奶 | ✅ 表内条目 | 平 +1 → **温** |
| 冰红茶 | 红茶 | ✅ 表内条目 | 温 -1 → **平** |
| 去冰奶茶 | 奶茶 | ✅ 表内条目 | 平 -1 → **凉** |
| 冰淇淋 | 淇淋 | ❌ 不是 | 温度字属于菜名，**不叠加** |
| 热狗 | 狗 | ❌ 不是 | 落到表外 → LLM 推测 |
| 热干面 | 干面 | ❌ 不是 | 靠关键词命中「面条」，**不升温** |
| 凉皮 / 凉茶 | — | 非前缀 | 各自查表 |

前缀表见 `app/domain/nature_math.py`，测试见 `tests/test_temperature_layer.py`。

**已知局限**：本层读**食物名**里的温度字。若模型把温度信息放进 `note`
而不是 `name`（实测「喝了杯去冰奶茶」会输出 `name=奶茶, note=去冰`），
本层看不到该信息，会退回表值。要覆盖这种情况，可以：

- 让 Agent1 提示词要求把温度字保留在 `name` 里（改动小，但有不确定性），或
- 解析时同时扫描 `note` 字段做同样的前缀判定（改动稍大，更可靠）。

目前按"表值优先"处理，不去猜 `note` 的语义。

## 置信度阈值

| 来源 | 置信度 | 界面行为 |
|---|---|---|
| 表命中且**已审核**（`approved`） | 0.9 | 不标注 |
| 表命中但**待审核**（`pending`） | 0.9 | 标「待验证」 |
| 组合推理 / 烹饪修正 | 0.6 | 标「待验证」 |
| 审核不通过（`rejected`） | 0.6 | 标「待验证」 |
| LLM 推测 | 0.3 | 标「待验证」 |
| 无法判定 | 0.1 | 标「待验证」+ **不显示**寒热属性 |

整餐把握度 = 逐项置信度均值，不再采信模型自报的 `confidence`。

## 审核状态（三态）

`reviewed: false` 无法区分"还没审"和"审了但不认可"，所以用三态：

| 状态 | 含义 | 效果 |
|---|---|---|
| `approved` | 已审核通过 | 真·硬规则库，不标注 |
| `pending` | 待审核（默认） | 高置信度但必须标注 |
| `rejected` | 审核不通过 | 降为组合推理档，不再作硬规则 |

当前表内 143 条全部为 `pending`，所以**界面会普遍显示「待验证」标记**——
这是刻意的：标记密度就是审核进度的可见反馈。

## 审核工作流

```powershell
# 迁移/维护脚本
python scripts\add_review_fields.py      # 补逐条审核字段
python scripts\add_food_entries.py       # 加新条目（幂等）
python scripts\patch_food_table.py       # 结构升级与条目修正（幂等）
```

把表交给审核人后，逐条把 `review_status` 改为 `approved`，
并填写 `reviewed_by` / `reviewed_at`。改完跑一次回归：

```powershell
python -m pytest -q                      # 133 项
python scripts\test_food_accuracy.py     # 真实模型 20 条语料 / 26 项断言
```

## 已知局限（重要，不要误当 bug 修）

1. **规范名精确命中时，不叠加模型给的烹饪修正。**
   因为无法从模型输出可靠区分"可靠证据"与"猜测"——模型可能给「希腊酸奶」一个
   `cold`、给「火腿三明治」一个 `grilled`，照单全收会把表值污染成错误档位。

   > 这一局限已由**温度前缀层**部分解决：「冰啤酒」「热牛奶」这类温度信息
   > 改用纯字符串规则判定，不再依赖模型。剩下的煎炸/烧烤类修正仍受此限制，
   > 需要时应在表里为该食物单列条目并写 `variant_nature`。

2. **关键词子串会把无关名称拉进表。**
   例如「某个沙拉卷」含"沙拉"→ 判为表内「沙拉」。「薯条」曾被匹配到
   「炸鱼薯条」、「寿司」曾被匹配到「生鱼片」。已通过三级匹配优先级
   （精确同名 > 包含关系按 gap 最小 > 关键词）修掉已知案例，
   但字符串匹配本质上无法完全消除这类歧义。要彻底解决需要菜品别名库或分词。

3. **第二层组合推理尚未接入主流程。**
   `combine()` 已实现并测试，但 `resolve_food` 目前只做单食材判定。
   「番茄炒蛋」这类整菜名要么表里收录，要么整体落到第三层。

4. **第一层数据尚未人工审核。**
   `data/food_properties.json` 全部为 `pending`。属性依据中医饮食养生通行表述整理
   （食物偏性，非药物功效），**上线前应由具备资质的中医师/中药师复核**。

## 测试

| 文件 | 覆盖内容 |
|---|---|
| `tests/test_nature_math.py` | 编码、夹取、边界取整、两层修正、合成算法（48 项） |
| `tests/test_food_lookup.py` | 表完整性、匹配准确性、渲染、名称对齐 |
| `tests/test_calibration.py` | 校准接线、来源标记、Agent2 隔离、已知局限固化 |
| `scripts/test_food_accuracy.py` | 真实模型端到端，20 条语料 / 26 项属性断言 |

# 统一行尾：文本走 LF，避免 Windows 上出现"整文件被改动"的假 diff
* text=auto eol=lf

# Windows 批处理必须 CRLF：cmd.exe 对 LF-only 的 goto/label 处理有歧义
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.db binary
*.zip binary
